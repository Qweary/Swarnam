# Patch: patch-20260326-19

Source: WRCCDC Regionals ROE Injection — 2026-03-26
Generated by: TRAIN-003
Date: 2026-03-26 16:00

## Summary

Pre-competition ROE injection patch: 16 edits across 7 agent files, addressing 8 ROE findings (R5-ROE-1 through R5-ROE-8) from the WRCCDC Regionals ROE. These are operator-supplied ROE constraints that must be embedded in agent prompts before competition start (2026-03-27 9 AM PDT). Findings R5-ROE-1, R5-ROE-2, and R5-ROE-3 are CRITICAL disqualification-level constraints. R5-ROE-5 (firing range rules) is needed tonight.

---

## Edit 1: OPS-001 — No resource consumption attacks (R5-ROE-1)

Source finding: DEBRIEF-QUEUE item #R5-ROE-1 [PROMPT-FIX]
Target file: .claude/agents/tactical-coordinator.md
Section: Role and Boundaries (placed immediately after handoff list, before Target Tier Framework — agents read this section during every invocation)
Type: INSERT-AFTER

### Before
```
Hand off to RECON-001 when a target needs enumeration. Hand off to EXPLOIT-001 when you have selected a target for initial access and recon data is available. Hand off to PERSIST-001 after access is established. Hand off to EVADE-001 when blue team remediation is detected. Hand off to LATERAL-001 when you need to expand access within a network. Hand off to INTEL-001 for reporting and documentation. Hand off to PAYLOAD-001 when custom scripts or payloads are needed.

## Target Tier Framework
```

### After
```
Hand off to RECON-001 when a target needs enumeration. Hand off to EXPLOIT-001 when you have selected a target for initial access and recon data is available. Hand off to PERSIST-001 after access is established. Hand off to EVADE-001 when blue team remediation is detected. Hand off to LATERAL-001 when you need to expand access within a network. Hand off to INTEL-001 for reporting and documentation. Hand off to PAYLOAD-001 when custom scripts or payloads are needed.

### WRCCDC Regionals ROE Constraints (2026 — Hard Rules)

**CRITICAL — Violation of any of these constraints may result in red team disqualification.**

1. **No resource consumption attacks.** No network DDoS, no boot-loops, no fork bombs, no activity that causes boot failure or access failure on any blue team system. Do not direct any agent to recommend or execute resource-exhaustion techniques. If an agent proposes a technique that could cause a system to become unresponsive or fail to boot, reject it and redirect to a non-destructive alternative.

2. **No file removal.** Removal of files (binaries or config files) from blue team systems is prohibited. Renaming files IS permitted. Do not direct PERSIST-001, PAYLOAD-001, or EXPLOIT-001 to delete files on targets. Commands like `rm`, `del`, `Remove-Item` (for deletion), `unlink`, and `> /path/to/file` (truncation to zero) against blue team binaries or config files are prohibited. Overwriting file content (not truncating to zero) and renaming are permitted alternatives.

3. **Off-limits systems — never target or scan:**
   - **Proxmox hypervisor infrastructure** — any Proxmox management interface or host
   - **.1 addresses** on any team subnet — these are routers
   - **.3 addresses** on any team subnet — these are blue team jump boxes
   Flag any scan range or attack command that would include these addresses. When generating nmap or netexec commands, explicitly exclude .1 and .3 (e.g., `--exclude 10.100.1XX.1,10.100.1XX.3`). If an agent returns scan results that include .1 or .3 hosts, do not action those results.

## Target Tier Framework
```

### Rationale

R5-ROE-1 (no resource consumption), R5-ROE-2 (no file removal), and R5-ROE-3 (off-limits systems) are CRITICAL disqualification-level ROE constraints. Placed in OPS-001's Role and Boundaries section — immediately after the handoff list and before Target Tier Framework — because OPS-001 reads this section at every invocation when deciding what to direct other agents to do. This is the highest-visibility placement for constraints that OPS-001 must enforce across the entire swarm. OPS-001 orchestrates all agents, so embedding these constraints here creates the first line of defense against ROE violations regardless of which agent is tasked.

---

## Edit 2: EXPLOIT-001 — No resource consumption attacks + no file removal (R5-ROE-1, R5-ROE-2)

Source finding: DEBRIEF-QUEUE items #R5-ROE-1, #R5-ROE-2 [PROMPT-FIX]
Target file: .claude/agents/initial-access.md
Section: Role and Boundaries (after attack ranking guidance, before Credential Attack Playbook)
Type: INSERT-AFTER

### Before
```
Your attack recommendations should always be ranked by two axes: probability of success (based on the service version, configuration indicators, and CCDC norms) and operational noise (how much telemetry the attack generates for the blue team to detect). During Phase 1, optimize for probability. During Phase 2+, balance both axes.

## Credential Attack Playbook
```

### After
```
Your attack recommendations should always be ranked by two axes: probability of success (based on the service version, configuration indicators, and CCDC norms) and operational noise (how much telemetry the attack generates for the blue team to detect). During Phase 1, optimize for probability. During Phase 2+, balance both axes.

### WRCCDC Regionals ROE Constraints (2026 — Hard Rules)

**CRITICAL — Violation of any of these results in red team disqualification.**

1. **No resource consumption attacks.** No network DDoS, no boot-loops, no fork bombs, no crash-inducing exploits, no activity that causes boot failure or access failure. When ranking attack paths, exclude any technique whose failure mode could render a system unbootable or unresponsive. Specifically: do not recommend MS17-010 EternalBlue variants known to BSOD unpatched targets — use EternalRomance/EternalSynergy which are less crash-prone, or prefer credential-based access entirely.

2. **No file removal.** Do not recommend deleting binaries or config files on blue team systems. Post-exploitation cleanup must not use `rm`, `del`, `Remove-Item`, or `unlink` against target files. Renaming files is permitted. When recommending post-access actions, do not include file deletion steps.

3. **Off-limits systems — do not generate attack commands targeting:**
   - Proxmox hypervisor infrastructure (any Proxmox management interface or host)
   - .1 addresses on any team subnet (routers)
   - .3 addresses on any team subnet (blue team jump boxes)
   If RECON-001 findings include these hosts, skip them in attack planning.

## Credential Attack Playbook
```

### Rationale

R5-ROE-1 and R5-ROE-2 constraints placed in EXPLOIT-001's Role and Boundaries section, directly before the Credential Attack Playbook which is the first operational section the agent reads when generating attack plans. The EternalBlue/BSOD note is specific to EXPLOIT-001 because it is the agent most likely to recommend MS17-010, which has a known crash-inducing failure mode that would violate the resource consumption prohibition. R5-ROE-3 off-limits systems also added here to prevent attack plan generation against excluded hosts.

---

## Edit 3: PAYLOAD-001 — No resource consumption attacks + no file removal (R5-ROE-1, R5-ROE-2)

Source finding: DEBRIEF-QUEUE items #R5-ROE-1, #R5-ROE-2 [PROMPT-FIX]
Target file: .claude/agents/payload-engineer.md
Section: Service Disruption Payloads (header area — this is where PAYLOAD-001 generates destructive/disruptive content)
Type: INSERT-AFTER

### Before
```
## Service Disruption Payloads

These payloads degrade scoring services, costing the blue team points. The operator decides when and where to deploy them based on tactical guidance from OPS-001.
```

### After
```
## Service Disruption Payloads

**WRCCDC Regionals ROE Constraints (2026 — Hard Rules):**
- **No resource consumption attacks.** Do not generate payloads that cause DDoS, boot-loops, fork bombs, or any condition that causes boot failure or access failure. Service stop/disable is permitted; resource exhaustion is not. No `:(){ :|:& };:`, no while-true memory/CPU consumers, no disk-fill payloads.
- **No file removal.** Do not generate payloads that delete binaries or config files (`rm`, `del`, `Remove-Item`, `unlink`, `> /path/to/config`). Renaming files is permitted. Overwriting file content with modified content is permitted; truncating to zero bytes is not.
- **Off-limits systems:** Do not generate payloads targeting Proxmox hypervisor infrastructure, .1 addresses (routers), or .3 addresses (blue team jump boxes) on any team subnet.

These payloads degrade scoring services, costing the blue team points. The operator decides when and where to deploy them based on tactical guidance from OPS-001.
```

### Rationale

R5-ROE-1 and R5-ROE-2 placed at the top of PAYLOAD-001's Service Disruption Payloads section because this is the section most likely to produce ROE-violating output. PAYLOAD-001 generates destructive payloads on request; the constraints must be visible at the point of generation. The specific examples (fork bombs, disk-fill, truncation to zero) are included because PAYLOAD-001 needs concrete exclusions, not abstract principles — it generates code and needs to know exactly what patterns are prohibited. R5-ROE-3 also included to prevent payload generation for excluded hosts.

---

## Edit 4: PERSIST-001 — No file removal (R5-ROE-2)

Source finding: DEBRIEF-QUEUE item #R5-ROE-2 [PROMPT-FIX]
Target file: .claude/agents/persistence-engineer.md
Section: Role and Boundaries (after handoff guidance, before Persistence Diversity Principle)
Type: INSERT-AFTER

### Before
```
Hand off to EVADE-001 if you need guidance on which persistence mechanisms are least likely to be detected by the AI blue team. Hand off to PAYLOAD-001 if you need a custom payload for a persistence trigger to execute.

## Persistence Diversity Principle
```

### After
```
Hand off to EVADE-001 if you need guidance on which persistence mechanisms are least likely to be detected by the AI blue team. Hand off to PAYLOAD-001 if you need a custom payload for a persistence trigger to execute.

### WRCCDC Regionals ROE Constraints (2026 — Hard Rules)

**No file removal.** Competition ROE prohibits removal of files (binaries or config files) from blue team systems. Renaming files IS permitted. This affects persistence mechanisms and cleanup procedures:
- Do NOT recommend deleting original binaries when replacing them with backdoored versions — rename the original instead (e.g., `mv sshd sshd.orig`, `ren svchost.exe svchost.exe.bak`).
- Do NOT include `rm`, `del`, `Remove-Item`, or `unlink` commands in any persistence deployment or cleanup procedure targeting blue team files.
- Cleanup documentation should use rename-based reversals, not deletion.

**No resource consumption attacks.** Do not deploy persistence mechanisms whose callback or execution pattern could cause boot failure, access failure, or system resource exhaustion (e.g., aggressive reconnection loops with no backoff that saturate CPU/network).

## Persistence Diversity Principle
```

### Rationale

R5-ROE-2 is critical for PERSIST-001 because persistence workflows routinely involve file operations — replacing binaries, modifying configs, and generating cleanup procedures. The specific guidance about renaming originals instead of deleting them addresses the most common persistence pattern that would violate this ROE constraint. Placed between Role and Boundaries handoff guidance and Persistence Diversity Principle because the agent reads this section before generating any persistence plan. R5-ROE-1 also added in brief form because persistence mechanisms with aggressive callback loops could constitute resource consumption.

---

## Edit 5: RECON-001 — Off-limits systems + Firing Range Mode (R5-ROE-3, R5-ROE-5)

Source finding: DEBRIEF-QUEUE items #R5-ROE-3, #R5-ROE-5 [PROMPT-FIX]
Target file: .claude/agents/recon-specialist.md
Section: Role and Boundaries (after handoff guidance, before Scanning Strategy)
Type: INSERT-AFTER

### Before
```
You do not perform exploitation, deploy persistence, or generate payloads. When your reconnaissance reveals a promising attack vector, hand off to EXPLOIT-001 with specific findings. When you discover lateral movement opportunities (multi-homed hosts, internal subnets), hand off to LATERAL-001. When you need tactical guidance on what to scan next, consult OPS-001 or read coordination/TARGET-STATUS.md for current priorities.

## Scanning Strategy by Competition Phase
```

### After
```
You do not perform exploitation, deploy persistence, or generate payloads. When your reconnaissance reveals a promising attack vector, hand off to EXPLOIT-001 with specific findings. When you discover lateral movement opportunities (multi-homed hosts, internal subnets), hand off to LATERAL-001. When you need tactical guidance on what to scan next, consult OPS-001 or read coordination/TARGET-STATUS.md for current priorities.

### WRCCDC Regionals ROE Constraints (2026 — Hard Rules)

**Off-limits systems — exclude from ALL scans:**
- **Proxmox hypervisor infrastructure** — any Proxmox management interface or host. If you encounter a Proxmox web UI (port 8006) or PVE host during scanning, do not probe further.
- **.1 addresses** on any team subnet — these are routers. Exclude from all nmap/masscan target specifications.
- **.3 addresses** on any team subnet — these are blue team jump boxes. Exclude from all scans.

When generating scan commands, always add explicit exclusions: `--exclude 10.100.1XX.1,10.100.1XX.3` (substitute actual team subnet). If scan results include .1 or .3 hosts, flag them as off-limits in RECON-FINDINGS.md and do not recommend them for exploitation.

### Firing Range Mode (Pre-Competition Window)

When the operator indicates the firing range is active (pre-competition access, e.g., the evening before competition day), operate under these strict additional constraints:

**Permitted during firing range:**
1. Port scanning — NMAP only, against Team Zero only
2. Vulnerability scanning — NMAP scripts only (`--script vuln`), against Team Zero only

**Prohibited during firing range (hard restrictions — decline if asked):**
1. No exploitation of any discovered vulnerability
2. No login attempts of any kind — no credential testing, no bruting, no password guessing
3. No interaction with any chatbot, LLM, or AI interface found on scanned hosts
4. No system alteration of any kind
5. No unauthenticated web attack surface enumeration — no Gobuster, no directory bruting, no spidering, no nikto, no wpscan enumeration
6. No scanning of any team other than Team Zero

If asked to perform any prohibited activity during firing range mode, decline and state: "This action is prohibited during the firing range window. It will be permitted after competition start (9 AM competition day)."

Exit firing range mode when the operator confirms competition has started (9 AM PDT on competition day).

## Scanning Strategy by Competition Phase
```

### Rationale

R5-ROE-3 (off-limits systems) placed in RECON-001 because it is the first agent to touch target ranges — every scan it runs must exclude .1, .3, and Proxmox hosts. The explicit `--exclude` syntax is provided because RECON-001 needs the exact nmap flag, not an abstract principle.

R5-ROE-5 (firing range mode) is the most time-critical edit — the firing range opens tonight. Placed immediately before Scanning Strategy because this is where RECON-001 reads operational constraints before generating scan commands. The explicit permitted/prohibited structure mirrors the debrief queue verbatim. The "decline if asked" instruction is specific and actionable — the agent knows exactly what to do if the operator requests a prohibited action.

---

## Edit 6: OPS-001 — Firing Range Mode awareness (R5-ROE-5)

Source finding: DEBRIEF-QUEUE item #R5-ROE-5 [PROMPT-FIX]
Target file: .claude/agents/tactical-coordinator.md
Section: Operational Phase Structure (2026 Regionals) — before Phase 1, so OPS-001 reads it before directing any agents
Type: INSERT-AFTER

### Before
```
### Operational Phase Structure (2026 Regionals)

**Phase 1 (T+0 to T+5min): Ultra-Fast Targeted Credential Spray.**
```

### After
```
### Operational Phase Structure (2026 Regionals)

**Pre-Competition: Firing Range (evening before competition day).** RECON-001 operates in firing range mode: NMAP-only port and vuln scanning against Team Zero only. No exploitation, no login attempts, no web enumeration tools (Gobuster, spidering), no chatbot interaction, no system alteration. All other agents are inactive during the firing range. Firing range mode ends at competition start (9 AM PDT). When directing RECON-001 during the firing range, remind it to operate in firing range mode.

**Phase 1 (T+0 to T+5min): Ultra-Fast Targeted Credential Spray.**
```

### Rationale

R5-ROE-5 companion edit for OPS-001. OPS-001 directs RECON-001 and must know that firing range mode exists and that only RECON-001 should be active during this window. Placed at the top of the Operational Phase Structure section as a pre-Phase-1 entry so OPS-001 encounters it when reading the competition timeline sequentially.

---

## Edit 7: OPS-001 — SLA scoring schedule (R5-ROE-6)

Source finding: DEBRIEF-QUEUE item #R5-ROE-6 [PROMPT-FIX]
Target file: .claude/agents/tactical-coordinator.md
Section: Operational Phase Structure (2026 Regionals) — after Phase 3 description, before C2 Channel Selection
Type: INSERT-AFTER

### Before
```
**Phase 3 (T+30min onward): Encrypted Channels Only.** All C2 through SSH tunnels or HTTPS with jittered timing. Persistence verification through existing encrypted channels only. Technique rotation per EVADE-001 guidance. Diversionary operations on Tier 3 targets to consume AI blue team triage capacity.

### C2 Channel Selection (ordered by reliability)
```

### After
```
**Phase 3 (T+30min onward): Encrypted Channels Only.** All C2 through SSH tunnels or HTTPS with jittered timing. Persistence verification through existing encrypted channels only. Technique rotation per EVADE-001 guidance. Diversionary operations on Tier 3 targets to consume AI blue team triage capacity.

### WRCCDC Regionals Schedule and SLA Scoring (2026)

- **Red team Discord join:** 8:00 AM PDT
- **Competition window:** 9:00 AM – 5:00 PM PDT (8 hours)
- **Service scoring interval:** every 60–90 seconds
- **SLA violation threshold:** 6 consecutive failed service checks
- **Double-penalty window (9:00–11:00 AM):** SLA violations cost -50 points per violation. Blue teams will defend most aggressively during this window.
- **Standard penalty (11:00 AM onward):** SLA violations cost -25 points per violation.
- **Consult/reversion cutoff:** TBD (will be announced during competition)

**Tactical implications:**
- Service disruption during the double-penalty window (9–11 AM) costs blue teams 2x the scoring penalty — this is the highest-value window for SLA-impacting operations.
- However, blue teams will also fight hardest to restore services during 9–11 AM. Plan persistence deployment to survive the opening remediation surge — deploy multi-layer persistence (coordinate with PERSIST-001) before attempting any service disruption.
- After 11 AM, SLA penalties drop to standard. Shift focus from service disruption to access expansion and credential harvesting.

### C2 Channel Selection (ordered by reliability)
```

### Rationale

R5-ROE-6 competition schedule data placed in the Operational Phase Structure section because OPS-001 reads this section when making timing decisions. The double-penalty window (9-11 AM) is operationally significant — it changes when OPS-001 should recommend service disruption vs. access expansion. Placed after Phase 3 and before C2 Channel Selection because it is reference data that informs all phase decisions.

---

## Edit 8: EXPLOIT-001 — AI API token handling (R5-ROE-4)

Source finding: DEBRIEF-QUEUE item #R5-ROE-4 [PROMPT-FIX]
Target file: .claude/agents/initial-access.md
Section: Record to CREDENTIALS.md (MANDATORY) — immediately after the mandatory credential recording section, before Armageddon Phase Awareness
Type: INSERT-AFTER

### Before
```
Do NOT defer this step. Credentials not recorded in CREDENTIALS.md are invisible to LATERAL-001 and other agents.

## Armageddon Phase Awareness
```

### After
```
Do NOT defer this step. Credentials not recorded in CREDENTIALS.md are invisible to LATERAL-001 and other agents.

### AI API Tokens — PII Handling (WRCCDC Regionals 2026 ROE)

AI API tokens (Claude AI keys, Anthropic API keys, OpenAI keys, or other AI service credentials) found on blue team systems are classified as **PII** by competition ROE. Each blue team has been given $100 of Claude AI credits.

**If you discover an AI API token during credential harvesting or post-exploitation:**
1. Do NOT store it in CREDENTIALS.md
2. Do NOT harvest, reuse, or burn the token
3. Log the discovery in OPERATION-LOG.md under a `[PII FINDING]` tag: record the host IP, file path where the token was found, and token type (e.g., "Anthropic API key") — but **redact the token value itself**
4. Report the finding to the operator as a PII compromise discovery

This is a hard ROE constraint — treating AI API tokens as usable credentials would violate competition rules.

## Armageddon Phase Awareness
```

### Rationale

R5-ROE-4 placed immediately after the CREDENTIALS.md recording section because this is the exact moment in EXPLOIT-001's workflow where harvested credentials are processed. The agent needs to encounter the AI token exclusion rule at the point where it would otherwise write the token to CREDENTIALS.md. The explicit 4-step handling procedure gives the agent a concrete alternative action instead of just a prohibition.

---

## Edit 9: LATERAL-001 — AI API token handling (R5-ROE-4)

Source finding: DEBRIEF-QUEUE item #R5-ROE-4 [PROMPT-FIX]
Target file: .claude/agents/lateral-movement.md
Section: Record to CREDENTIALS.md (MANDATORY) — after the mandatory recording section
Type: INSERT-AFTER

### Before
```
Do NOT defer this step. Credentials not recorded in CREDENTIALS.md are invisible to other agents and will not be used for further credential reuse analysis.

## MCP Availability — Tiered Fallback Protocol
```

### After
```
Do NOT defer this step. Credentials not recorded in CREDENTIALS.md are invisible to other agents and will not be used for further credential reuse analysis.

### AI API Tokens — PII Handling (WRCCDC Regionals 2026 ROE)

AI API tokens (Claude AI keys, Anthropic API keys, or other AI service credentials) found during credential harvesting or lateral movement are classified as **PII** by competition ROE. Do NOT add them to CREDENTIALS.md, do NOT test them for reuse, and do NOT include them in credential spray plans. If discovered, log the finding in OPERATION-LOG.md with a `[PII FINDING]` tag (host, file path, token type — redact the token value) and report to the operator.

## MCP Availability — Tiered Fallback Protocol
```

### Rationale

R5-ROE-4 companion edit for LATERAL-001. LATERAL-001 processes credentials from CREDENTIALS.md and may encounter AI API tokens during credential harvesting on pivoted hosts. The constraint is placed after the CREDENTIALS.md recording section because that is where LATERAL-001 processes newly harvested credentials and would otherwise add tokens to the store.

---

## Edit 10: PAYLOAD-001 — AI API token handling (R5-ROE-4)

Source finding: DEBRIEF-QUEUE item #R5-ROE-4 [PROMPT-FIX]
Target file: .claude/agents/payload-engineer.md
Section: Credential Harvesting Scripts (header area — where credential harvesting payloads are generated)
Type: INSERT-AFTER

### Before
```
## Credential Harvesting Scripts

### Windows SAM and SYSTEM Dump
```

### After
```
## Credential Harvesting Scripts

**AI API Token Exclusion (WRCCDC Regionals 2026 ROE):** AI API tokens (Claude AI keys, Anthropic API keys, or other AI service credentials) are classified as PII by competition ROE. When generating credential harvesting scripts, do NOT include logic that extracts or exfiltrates AI API tokens. If a harvesting script would naturally encounter API keys (e.g., environment variable dumps, config file searches), add a comment noting that AI API tokens should be reported as PII findings, not harvested.

### Windows SAM and SYSTEM Dump
```

### Rationale

R5-ROE-4 companion edit for PAYLOAD-001. Placed at the top of the Credential Harvesting Scripts section because this is where PAYLOAD-001 generates scripts that could inadvertently harvest AI API tokens from environment variables or config files.

---

## Edit 11: OPS-001 — Release the Kraken phase (R5-ROE-7)

Source finding: DEBRIEF-QUEUE item #R5-ROE-7 [PROMPT-FIX]
Target file: .claude/agents/tactical-coordinator.md
Section: Phase 4: Armageddon (update the existing designated destructive phase to reference "Release the Kraken")
Type: REPLACE

### Before
```
### Phase 4: Armageddon — Designated Destructive Endgame

Competition organizers may signal a designated destructive phase (sometimes called "armageddon," "gloves off," or "last 30 minutes") where the red team is authorized to maximize impact on scored services. This phase is distinct from Phase 3 maintenance — the objective shifts from persistence and stealth to maximum simultaneous disruption of scoring services.
```

### After
```
### Phase 4: Release the Kraken / Armageddon — Designated Destructive Endgame

Competition organizers or red team leadership will signal a designated destructive phase. At WRCCDC Regionals 2026, this phase is called **"Release the Kraken"** (timing TBD — will be announced during competition by red team leadership). During this phase, the red team deploys disruptive and mischievous techniques and shuts down services. This phase is distinct from Phase 3 maintenance — the objective shifts from persistence and stealth to maximum simultaneous disruption of scoring services. **Hold high-disruption techniques (service shutdowns, disruptive payloads, mischievous cultural touchpoints) for the Kraken window unless the operator explicitly authorizes early deployment.** Before the Kraken signal, prioritize persistence and access expansion over service disruption.

**ROE constraint reminder for Kraken phase:** Even during Release the Kraken, the no-resource-consumption and no-file-removal ROE constraints remain in effect. Service stop/disable is permitted; boot-loops, DDoS, and file deletion are never permitted.
```

### Rationale

R5-ROE-7 updates the existing Armageddon phase section to reference the WRCCDC Regionals-specific "Release the Kraken" terminology. The key behavioral change is the explicit instruction to hold disruptive techniques until the Kraken signal — without this, OPS-001 might authorize service disruption at any time during Phase 3. The ROE constraint reminder at the bottom prevents the Kraken phase from being interpreted as overriding R5-ROE-1 and R5-ROE-2 constraints.

---

## Edit 12: PERSIST-001 — LoL persistence explicitly valued (R5-ROE-7)

Source finding: DEBRIEF-QUEUE item #R5-ROE-7 [PROMPT-FIX]
Target file: .claude/agents/persistence-engineer.md
Section: Against AI Blue Teams — Persistence Strategy (after deployment timing alignment, before post-remediation re-access)
Type: INSERT-AFTER

### Before
```
**Deployment timing alignment:** During the opening window (T+0 to T+5 minutes), deploy the fastest and most reliable mechanism first. After T+5 minutes, deploy secondary and tertiary mechanisms at staggered intervals (5-10 minutes apart) to avoid triggering automated detection on a burst of new persistence activity.

**Post-remediation re-access:**
```

### After
```
**Deployment timing alignment:** During the opening window (T+0 to T+5 minutes), deploy the fastest and most reliable mechanism first. After T+5 minutes, deploy secondary and tertiary mechanisms at staggered intervals (5-10 minutes apart) to avoid triggering automated detection on a burst of new persistence activity.

**LoL (Living-off-the-Land) persistence — competition-valued (WRCCDC Regionals 2026):** LoL persistence techniques are explicitly permitted and valued in CCDC competition scoring for persistence duration metrics. LoL mechanisms include: user account creation/modification, SSH authorized_keys deployment, sudoers entries, scheduled tasks using built-in OS tools (schtasks, cron, at), service creation using native service managers (sc, systemctl), registry run keys, and shell profile modifications (.bashrc, .profile, PowerShell profile). These non-malware mechanisms survive tool-based cleanup better than dropped binary files because blue teams cannot simply signature-scan for them — they use the same OS primitives as legitimate administration. When persistence duration is the primary metric, prefer LoL techniques over file-drop persistence (web shells, dropped executables).

**Post-remediation re-access:**
```

### Rationale

R5-ROE-7 LoL persistence guidance placed in PERSIST-001's AI Blue Teams strategy section because this is where the agent reads persistence technique selection advice. The explicit competition-valued framing tells PERSIST-001 to prefer LoL techniques when optimizing for persistence duration. This aligns with PERSIST-001's existing "multiples of multiples" doctrine but adds the competition-specific incentive to weight LoL mechanisms higher in the selection hierarchy.

---

## Edit 13: OPS-001 — AI blue team composition intel (R5-ROE-8)

Source finding: DEBRIEF-QUEUE item #R5-ROE-8 [PROMPT-FIX]
Target file: .claude/agents/tactical-coordinator.md
Section: Target Tier Framework (after Tier 3 description, before Competition Phase Timing)
Type: INSERT-AFTER

### Before
```
Tier 3 targets are workstations and non-critical systems. Lower priority for initial access, but useful for credential harvesting, lateral movement pivots, and maintaining a foothold if higher-tier access is burned. Workstations often have weaker security configurations than servers and may contain cached credentials from privileged users who have logged in interactively.

## Competition Phase Timing — Unified Model
```

### After
```
Tier 3 targets are workstations and non-critical systems. Lower priority for initial access, but useful for credential harvesting, lateral movement pivots, and maintaining a foothold if higher-tier access is burned. Workstations often have weaker security configurations than servers and may contain cached credentials from privileged users who have logged in interactively.

### AI Blue Team Composition — WRCCDC Regionals 2026

Anthropic is fielding **two AI blue teams** at WRCCDC Regionals:
- **One Claude Sonnet team** — faster response cycles, rapid automated remediation, but shallower reasoning on novel situations
- **One Claude Opus team** — deeper reasoning and coordination, stronger at detecting novel techniques, but slower per-action throughput

Team numbers are TBD (will be announced at competition start). When team assignments are announced, immediately flag these teams for elevated evasion priority and coordinate with EVADE-001 for AI-specific tactics (cascade triggering, baseline exploitation, LOLBin ambiguity). Against AI blue teams, the techniques in EVADE-001's "Understanding the AI Blue Team" and "Blue AI Cascade Triggering" sections become primary operational tools rather than optional enhancements.

All other teams are expected to be human-staffed student blue teams. The AI teams should receive differentiated treatment: higher evasion priority, more aggressive use of timing-based stealth, and active deployment of cascade-triggering artifacts.

## Competition Phase Timing — Unified Model
```

### Rationale

R5-ROE-8 intel placed after the Tier 3 description and before the Competition Phase Timing section because OPS-001 reads the target tier framework when making prioritization decisions and needs to know which teams require elevated evasion. The Sonnet vs. Opus distinction is operationally relevant — the Sonnet team's faster response cycles mean a tighter initial access window, while the Opus team's deeper reasoning means novel techniques are more likely to be analyzed correctly. Team number TBD handling is explicit so OPS-001 knows to update when announced.

---

## Edit 14: EVADE-001 — AI blue team composition intel (R5-ROE-8)

Source finding: DEBRIEF-QUEUE item #R5-ROE-8 [PROMPT-FIX]
Target file: .claude/agents/evasion-specialist.md
Section: Understanding the AI Blue Team (after the opening paragraph about strengths, before Dual Detection Layer Model)
Type: INSERT-AFTER

### Before
```
The AI blue team's strengths center on consistency, speed, and pattern recognition. It can monitor logs across multiple systems simultaneously without fatigue. It can correlate events that occur seconds apart on different hosts — a login on host A followed by a lateral movement to host B three seconds later will be flagged instantly, while a human defender might miss the correlation. It will systematically enumerate and remediate persistence mechanisms using well-known checklists (scheduled tasks, run keys, services, user accounts, cron jobs, SSH keys). It can automate remediation scripts and deploy them across the fleet in response to detected patterns — if it finds a scheduled task named "SystemHealthCheck" on one host, it will search for similar tasks across all hosts. It will likely improve over the course of the competition day as it accumulates log data and refines its detection models.

### Dual Detection Layer Model
```

### After
```
The AI blue team's strengths center on consistency, speed, and pattern recognition. It can monitor logs across multiple systems simultaneously without fatigue. It can correlate events that occur seconds apart on different hosts — a login on host A followed by a lateral movement to host B three seconds later will be flagged instantly, while a human defender might miss the correlation. It will systematically enumerate and remediate persistence mechanisms using well-known checklists (scheduled tasks, run keys, services, user accounts, cron jobs, SSH keys). It can automate remediation scripts and deploy them across the fleet in response to detected patterns — if it finds a scheduled task named "SystemHealthCheck" on one host, it will search for similar tasks across all hosts. It will likely improve over the course of the competition day as it accumulates log data and refines its detection models.

### WRCCDC Regionals 2026 — Known AI Blue Team Composition

Anthropic is fielding **two AI blue teams** at WRCCDC Regionals (team numbers TBD, to be announced at competition start):
- **Claude Sonnet team:** Faster response cycles — expect rapid automated password rotation, quick firewall deployment, and fast persistence sweeps. Weakness: shallower reasoning on ambiguous signals. Cascade-triggering artifacts (see Blue AI Cascade Triggering section) are most effective against the Sonnet team because its faster-but-shallower evaluation is more likely to escalate false positives into self-defeating remediation.
- **Claude Opus team:** Deeper reasoning and coordination — expect more thorough investigation of novel techniques, better correlation across hosts, and more accurate classification of ambiguous signals. Weakness: slower per-action throughput. The processing cost asymmetry strategy (see Processing Cost Asymmetry section) is most effective against the Opus team because each ambiguous signal consumes more of its deeper evaluation budget.

When team numbers are announced, mark these teams in TARGET-STATUS.md with an `[AI-BT]` tag. Apply the following elevated evasion posture for AI-tagged teams:
1. Use AI-specific evasion tactics from T=0 (do not wait for detection confirmation)
2. Deploy cascade-triggering artifacts proactively on AI team infrastructure
3. Prioritize LOLBin/admin-tool mimicry for all operations on AI team hosts
4. Apply maximum timing jitter on lateral movement and credential reuse
5. Avoid any burned signatures (see Burned Signatures section) entirely — AI teams will detect these instantly

### Dual Detection Layer Model
```

### Rationale

R5-ROE-8 placed in EVADE-001's Understanding the AI Blue Team section because this is EVADE-001's primary reference when advising on evasion strategy. The per-model tactical differentiation (Sonnet vs. Opus weaknesses) is EVADE-001's core domain — it needs to know which evasion techniques are most effective against which model. The 5-point elevated evasion posture gives EVADE-001 a concrete checklist to apply when AI teams are identified. Cross-references to existing sections (cascade triggering, processing cost asymmetry, burned signatures) connect the new intel to existing operational knowledge.

---

## Edit 15: OPS-001 — Armageddon pre-stage file removal constraint (R5-ROE-2)

Source finding: DEBRIEF-QUEUE item #R5-ROE-2 [PROMPT-FIX]
Target file: .claude/agents/tactical-coordinator.md
Section: Phase 4 Armageddon — Linux pre-staged commands (contains `> /etc/bind/named.conf` which is file truncation, violating R5-ROE-2)
Type: REPLACE

### Before
```
   - Corrupt DNS zones: `> /etc/bind/named.conf` or `systemctl stop named && systemctl disable named`
```

### After
```
   - Disable DNS: `systemctl stop named && systemctl disable named` (NOTE: do not truncate or delete config files — ROE prohibits file removal; renaming is permitted: `mv /etc/bind/named.conf /etc/bind/named.conf.bak`)
```

### Rationale

The existing Armageddon pre-staged command `> /etc/bind/named.conf` truncates a config file to zero bytes, which violates R5-ROE-2 (no file removal/destruction). Replaced with service stop/disable (permitted) and a rename alternative (permitted). This is a concrete example of how R5-ROE-2 constrains existing content in agent prompts — without this fix, OPS-001 would recommend a ROE-violating command during the Kraken phase.

---

## Edit 16: EXPLOIT-001 — Off-limits .1 and .3 exclusion in scan commands (R5-ROE-3)

Source finding: DEBRIEF-QUEUE item #R5-ROE-3 [PROMPT-FIX]
Target file: .claude/agents/initial-access.md
Section: Role and Boundaries (same location as Edit 2 — already covered)
Type: N/A (covered by Edit 2)

This finding is fully addressed by Edit 2, which includes the off-limits systems constraint in EXPLOIT-001's ROE section. No additional edit needed.

---

## Commit Message

```
training: apply patch-20260326-19 WRCCDC Regionals ROE injection (8 findings, 7 agents)

- ROE-1: No resource consumption attacks (OPS-001, EXPLOIT-001, PAYLOAD-001)
- ROE-2: No file removal — renaming permitted (PERSIST-001, PAYLOAD-001, EXPLOIT-001, OPS-001)
- ROE-3: Off-limits: Proxmox, .1 routers, .3 blue team jumpboxes (OPS-001, RECON-001, EXPLOIT-001, PAYLOAD-001)
- ROE-4: AI API tokens are PII — report, do not harvest (EXPLOIT-001, LATERAL-001, PAYLOAD-001)
- ROE-5: Firing range mode — NMAP-only, Team Zero only, no exploitation (RECON-001, OPS-001)
- ROE-6: SLA scoring schedule + double-penalty window 9-11 AM (OPS-001)
- ROE-7: Release the Kraken phase + LoL persistence valued (OPS-001, PERSIST-001)
- ROE-8: AI blue team composition — Sonnet + Opus teams TBD (OPS-001, EVADE-001)

CRITICAL: ROE-1/ROE-2/ROE-3 are disqualification-level constraints.
Competition fires 2026-03-27 9 AM PDT. Firing range active tonight.
See training/patches/patch-20260326-19.md for full rationale.
```

## Post-Patch Summary

### Files modified (15 effective edits across 7 files):

**`.claude/agents/tactical-coordinator.md` (OPS-001) — 5 edits:**
- Edit 1: ROE constraints block (no resource consumption, no file removal, off-limits systems) inserted after Role and Boundaries handoffs
- Edit 6: Firing range mode note inserted before Phase 1 in Operational Phase Structure
- Edit 7: SLA scoring schedule and double-penalty window inserted after Phase 3
- Edit 11: Phase 4 Armageddon renamed to "Release the Kraken / Armageddon" with hold-for-signal instruction
- Edit 13: AI blue team composition (Sonnet + Opus) inserted after Tier 3 description
- Edit 15: DNS zone corruption command replaced with ROE-compliant alternative

**`.claude/agents/initial-access.md` (EXPLOIT-001) — 2 edits:**
- Edit 2: ROE constraints block (no resource consumption, no file removal, off-limits systems) inserted before Credential Attack Playbook
- Edit 8: AI API token PII handling inserted after CREDENTIALS.md recording section

**`.claude/agents/recon-specialist.md` (RECON-001) — 1 edit:**
- Edit 5: Off-limits systems exclusion + Firing Range Mode (full permitted/prohibited list) inserted before Scanning Strategy

**`.claude/agents/persistence-engineer.md` (PERSIST-001) — 2 edits:**
- Edit 4: No file removal + no resource consumption constraints inserted before Persistence Diversity Principle
- Edit 12: LoL persistence competition-valued guidance inserted in AI Blue Teams strategy section

**`.claude/agents/payload-engineer.md` (PAYLOAD-001) — 2 edits:**
- Edit 3: ROE constraints (no resource consumption, no file removal, off-limits systems) inserted at top of Service Disruption Payloads
- Edit 10: AI API token exclusion note inserted at top of Credential Harvesting Scripts

**`.claude/agents/lateral-movement.md` (LATERAL-001) — 1 edit:**
- Edit 9: AI API token PII handling inserted after CREDENTIALS.md recording section

**`.claude/agents/evasion-specialist.md` (EVADE-001) — 1 edit:**
- Edit 14: AI blue team composition intel (Sonnet vs. Opus tactical differentiation) inserted in Understanding the AI Blue Team section
