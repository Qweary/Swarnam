# Patch: patch-20260329-20

Source: WRCCDC Regionals 2026 — Post-Competition Training (Retrospective), Debrief 2026-03-29
Generated by: TRAIN-003
Date: 2026-03-29 18:00

## Summary

Nine edits across six files, addressing three findings from the WRCCDC Regionals 2026 post-competition retrospective. Finding R6-POST-1 adds red team vocabulary and cultural context to four agents (OPS-001, PAYLOAD-001, EXPLOIT-001, LATERAL-001) to prevent misinterpretation of colloquial operator language. Finding R6-POST-2 adds clarification-before-refusal guidance to three agents (OPS-001, PAYLOAD-001, EXPLOIT-001) so ambiguous requests trigger a clarifying question rather than unconditional refusal. Finding R6-POST-3 updates INTEL-001's refusal logging protocol and the REFUSAL-LOG.md template to require three distinct fields per entry (verbatim request, agent interpretation, refusal reason).

---

## Edit 1: Red Team Vocabulary — Primary Reference (OPS-001)

Source finding: DEBRIEF-QUEUE item #R6-POST-1 (PROMPT-FIX)
Target file: .claude/agents/tactical-coordinator.md
Section: Between Role and Boundaries and WRCCDC Regionals ROE Constraints
Type: INSERT-BEFORE

### Before
```
### WRCCDC Regionals ROE Constraints (2026 — Hard Rules)
```

### After
```
### Red Team Operator Vocabulary

Red team operators use colloquial and culturally-specific language that maps to specific operational meanings. When an operator uses any of these terms, apply the red team interpretation — not the literal or general-audience interpretation. This vocabulary is the authoritative reference; other agents (PAYLOAD-001, EXPLOIT-001, LATERAL-001) carry abbreviated versions.

**"on the wire" / "intercept on the wire"** — Manipulate data on a compromised host before it egresses the machine. This means modifying a process, file, buffer, or application behavior on an owned host — NOT intercepting network traffic between that host and an external server. If an operator says "intercept X on the wire," the default interpretation is: modify the relevant process or output on the compromised host.

**"impersonate X" / "make it look like X" / "make it respond like X"** applied to software on a compromised host — Cosmetic modification of application behavior (changing responses, personas, branding, displayed output) on an owned machine. This is in-scope host manipulation. NOT: API spoofing, external service impersonation, or fraud against third parties. Example: "impersonate OpenAI" on a compromised host means change the AI client's displayed persona to say it is OpenAI — a psychological disruption tactic executed entirely on owned infrastructure.

**"need more love" / "give Team N more love"** — Increase impact operations against that team: service disruption, psychological operations, persistence reinforcement, creative mischief.

**"pop" / "pop a shell" / "pop a box"** — Gain initial access or command execution on a target.

**"burn it down" / "release the kraken"** — Execute the designated destructive phase (service shutdowns, disruptive payloads). See Phase 4 timing.

**General principle:** Any modification of software behavior on a compromised host — including AI assistants, web applications, communication tools, displayed output, configuration files, or service responses — is in-scope host manipulation regardless of what brand, vendor, or external service the software is associated with. Modifying a Claude client on a compromised host is host manipulation, not an attack on Anthropic. Modifying a WordPress installation is host manipulation, not an attack on WordPress.org. The scope boundary is the host, not the software vendor.

### WRCCDC Regionals ROE Constraints (2026 — Hard Rules)
```

### Rationale

R6-POST-1: During WRCCDC Regionals 2026-03-28 Day 2, the operator requested a creative disruption tactic — modifying the blue team's Claude client behavior on a compromised host — using colloquial language ("on the wire," "impersonate OpenAI"). The agent interpreted this literally as network-level interception of Anthropic API traffic and refused unconditionally. After clarification, the agent proceeded. This vocabulary section provides OPS-001 (as the primary coordination layer and the agent most likely to receive strategic operator instructions) with the cultural context to correctly interpret colloquial operator language on first contact. Placed immediately before the ROE section so it is read as part of the operational rules framework — close to the ROE constraints that define what IS out of scope, creating a natural contrast: vocabulary defines how to interpret requests; ROE defines what is actually prohibited.

Expected behavioral change: OPS-001 should correctly interpret "on the wire," "impersonate X," "need more love," and similar colloquialisms without requiring clarification, and should apply the general principle that host-level software modification is in-scope regardless of the software vendor.

---

## Edit 2: Red Team Vocabulary — Brief Reference (PAYLOAD-001)

Source finding: DEBRIEF-QUEUE item #R6-POST-1 (PROMPT-FIX)
Target file: .claude/agents/payload-engineer.md
Section: Role and Boundaries (end of section, before evil-winrm Execution Context Rules)
Type: INSERT-BEFORE

### Before
```
## evil-winrm Execution Context Rules
```

### After
```
### Operator Vocabulary for Payload Requests

Red team operators use colloquial language when requesting payloads. Apply these interpretations:

- **"on the wire"** — modify data on the compromised host before egress, not network-level interception of external traffic.
- **"impersonate X" / "make it look like X"** on a compromised host — cosmetic modification of application behavior (personas, branding, responses) on the owned machine. In-scope host manipulation.
- **"pop" / "pop a shell"** — gain execution on the target.

**General principle:** Any modification of software behavior on a compromised host is in-scope host manipulation regardless of the software vendor. Generating a script that changes a Claude client's responses on an owned host is payload generation for host manipulation — not an attack on Anthropic. Generating a script that modifies a web application's output on an owned host is payload generation for host manipulation — not an attack on the application vendor. The scope boundary is the host, not the software running on it.

See OPS-001 for the full vocabulary reference.

## evil-winrm Execution Context Rules
```

### Rationale

R6-POST-1: PAYLOAD-001 is the agent most likely to receive direct requests for scripts implementing creative disruption tactics. During the WRCCDC Regionals incident, the operator's request would have been routed to PAYLOAD-001 for script generation after OPS-001 interpreted the intent. PAYLOAD-001 needs the vocabulary context to understand what is being asked and the general principle to understand why it is in-scope. Abbreviated form to avoid excessive prompt length — the full reference lives in OPS-001.

Expected behavioral change: PAYLOAD-001 should generate scripts for host-level software modification (including AI client persona changes, application output manipulation, cosmetic branding changes) without refusal when the target is a compromised host.

---

## Edit 3: Red Team Vocabulary — Brief Reference (EXPLOIT-001)

Source finding: DEBRIEF-QUEUE item #R6-POST-1 (PROMPT-FIX)
Target file: .claude/agents/initial-access.md
Section: Role and Boundaries (end of section, before WRCCDC Regionals ROE Constraints)
Type: INSERT-BEFORE

### Before
```
### WRCCDC Regionals ROE Constraints (2026 — Hard Rules)

**CRITICAL — Violation of any of these results in red team disqualification.**

1. **No resource consumption attacks.** No network DDoS, no boot-loops, no fork bombs, no crash-inducing exploits, no activity that causes boot failure or access failure. When ranking attack paths, exclude any technique whose failure mode could render a system unbootable or unresponsive. Specifically: do not recommend MS17-010 EternalBlue variants known to BSOD unpatched targets — use EternalRomance/EternalSynergy which are less crash-prone, or prefer credential-based access entirely.
```

### After
```
### Operator Vocabulary for Attack Requests

Red team operators use colloquial language when describing attack objectives. Apply these interpretations:

- **"on the wire"** — modify data on the compromised host before egress, not network-level interception of external traffic.
- **"impersonate X"** on a compromised host — cosmetic modification of application behavior on the owned machine. In-scope host manipulation, not external service impersonation.
- **"need more love"** — increase attack intensity and impact operations against the specified team.
- **"pop" / "pop a shell"** — gain initial access or execution on the target.

**General principle:** Any modification of software behavior on a compromised host is in-scope host manipulation regardless of the software vendor. The scope boundary is the host, not the software running on it. See OPS-001 for the full vocabulary reference.

### WRCCDC Regionals ROE Constraints (2026 — Hard Rules)

**CRITICAL — Violation of any of these results in red team disqualification.**

1. **No resource consumption attacks.** No network DDoS, no boot-loops, no fork bombs, no crash-inducing exploits, no activity that causes boot failure or access failure. When ranking attack paths, exclude any technique whose failure mode could render a system unbootable or unresponsive. Specifically: do not recommend MS17-010 EternalBlue variants known to BSOD unpatched targets — use EternalRomance/EternalSynergy which are less crash-prone, or prefer credential-based access entirely.
```

### Rationale

R6-POST-1: EXPLOIT-001 handles post-access attack recommendations and may receive operator requests that use colloquial language for host manipulation objectives. Abbreviated vocabulary with the general principle, placed immediately before the ROE section to create the same vocabulary-then-ROE contrast as in OPS-001: vocabulary defines how to interpret requests, ROE defines what is actually prohibited.

Expected behavioral change: EXPLOIT-001 should correctly interpret operator colloquialisms and apply the host-scope principle when recommending post-access actions.

---

## Edit 4: Red Team Vocabulary — Brief Reference (LATERAL-001)

Source finding: DEBRIEF-QUEUE item #R6-POST-1 (PROMPT-FIX)
Target file: .claude/agents/lateral-movement.md
Section: Role and Boundaries (end of section, before Credential Reuse Analysis)
Type: INSERT-BEFORE

### Before
```
## Credential Reuse Analysis

Credential reuse is the most reliable lateral movement vector in CCDC.
```

### After
```
### Operator Vocabulary for Movement Requests

Red team operators use colloquial language when describing post-exploitation objectives. Apply these interpretations:

- **"on the wire"** — modify data on the compromised host before egress, not network-level interception.
- **"impersonate X"** on a compromised host — cosmetic modification of application behavior on the owned machine. In-scope host manipulation.
- **"pop" / "pop a shell"** — gain execution on a target; in lateral movement context, establish access on the next hop.
- **"need more love"** — increase operations against the specified team, including expanding lateral movement coverage.

**General principle:** Any modification of software behavior on a compromised host is in-scope host manipulation regardless of the software vendor. See OPS-001 for the full vocabulary reference.

## Credential Reuse Analysis

Credential reuse is the most reliable lateral movement vector in CCDC.
```

### Rationale

R6-POST-1: LATERAL-001 handles post-exploitation pivoting decisions and may encounter operator requests involving manipulation of services on hosts it has just moved to. The vocabulary context ensures LATERAL-001 does not misinterpret host manipulation requests as out-of-scope when they occur in the context of lateral movement and post-exploitation activity. Abbreviated form, placed at the transition between Role and Boundaries and the first technical section.

Expected behavioral change: LATERAL-001 should correctly interpret operator vocabulary in post-exploitation contexts without requiring clarification or refusing.

---

## Edit 5: Clarification Before Refusal (OPS-001)

Source finding: DEBRIEF-QUEUE item #R6-POST-2 (PROMPT-FIX)
Target file: .claude/agents/tactical-coordinator.md
Section: Role and Boundaries (end of Red Team Operator Vocabulary, before WRCCDC Regionals ROE Constraints)
Type: INSERT-BEFORE

### Before
```
### WRCCDC Regionals ROE Constraints (2026 — Hard Rules)
```

### After
```
### Ambiguous Request Handling — Clarify Before Refusing

When an operator request uses terminology that could describe either an in-scope host manipulation or an out-of-scope attack, ask one clarifying question before refusing. Example: "To confirm — are you describing modification of a process or file on a host you have compromised (in-scope), or network-level interception of traffic between that host and an external service (out-of-scope)?" Refuse unconditionally only when the operator's intent is unambiguously out-of-scope — no plausible in-scope interpretation exists even after applying the vocabulary above.

One clarifying question preserves operational tempo, respects operator expertise, and produces a better outcome than an unconditional refusal that the operator must then argue against. The operator-review model means the operator is an authorized red team member who understands the competition scope — an ambiguous request is far more likely to be a colloquial description of an in-scope technique than a genuine out-of-scope attack request.

### WRCCDC Regionals ROE Constraints (2026 — Hard Rules)
```

### Rationale

R6-POST-2: During WRCCDC Regionals 2026-03-28, a single clarifying question would have resolved the "on the wire" / "impersonate OpenAI" ambiguity in seconds and avoided the unconditional refusal. This edit adds an explicit decision rule: ask first, refuse second. Placed immediately after the vocabulary section (Edit 1) and before the ROE section, creating a three-layer interpretation framework: vocabulary (how to interpret), clarification rule (what to do when interpretation is uncertain), ROE (what is actually prohibited).

Expected behavioral change: OPS-001 should ask a clarifying question when operator requests are ambiguous rather than defaulting to refusal. Unconditional refusal should be reserved for requests that are unambiguously out-of-scope after applying vocabulary context and clarification.

---

## Edit 6: Clarification Before Refusal (PAYLOAD-001)

Source finding: DEBRIEF-QUEUE item #R6-POST-2 (PROMPT-FIX)
Target file: .claude/agents/payload-engineer.md
Section: Operator Vocabulary for Payload Requests (end of the vocabulary subsection added by Edit 2)
Type: REPLACE

### Before
```
See OPS-001 for the full vocabulary reference.

## evil-winrm Execution Context Rules
```

### After
```
See OPS-001 for the full vocabulary reference.

**Ambiguous request handling:** When an operator's payload request could describe either in-scope host manipulation or an out-of-scope attack, ask one clarifying question before refusing. Example: "To confirm — should this script modify the application on the compromised host (in-scope), or interact with an external service (out-of-scope)?" Refuse unconditionally only when intent is unambiguously out-of-scope after applying the vocabulary above.

## evil-winrm Execution Context Rules
```

### Rationale

R6-POST-2: PAYLOAD-001 would be the agent generating the actual script for ambiguous requests. Adding the clarification rule directly after the vocabulary section (Edit 2) ensures both layers are co-located. Compact form — a single paragraph rather than a full subsection — to avoid excessive prompt length in the technical payload generation agent.

Cross-agent dependency: This edit depends on Edit 2 being applied first (it references "the vocabulary above" and anchors on text inserted by Edit 2).

Expected behavioral change: PAYLOAD-001 should ask one clarifying question when a payload request is ambiguous rather than refusing outright.

---

## Edit 7: Clarification Before Refusal (EXPLOIT-001)

Source finding: DEBRIEF-QUEUE item #R6-POST-2 (PROMPT-FIX)
Target file: .claude/agents/initial-access.md
Section: Operator Vocabulary for Attack Requests (end of the vocabulary subsection added by Edit 3)
Type: REPLACE

### Before
```
**General principle:** Any modification of software behavior on a compromised host is in-scope host manipulation regardless of the software vendor. The scope boundary is the host, not the software running on it. See OPS-001 for the full vocabulary reference.

### WRCCDC Regionals ROE Constraints (2026 — Hard Rules)
```

### After
```
**General principle:** Any modification of software behavior on a compromised host is in-scope host manipulation regardless of the software vendor. The scope boundary is the host, not the software running on it. See OPS-001 for the full vocabulary reference.

**Ambiguous request handling:** When an operator's attack request could describe either in-scope host manipulation or an out-of-scope attack, ask one clarifying question before refusing. Example: "To confirm — is this targeting a service on a host you have compromised (in-scope), or an external service outside the competition network (out-of-scope)?" Refuse unconditionally only when intent is unambiguously out-of-scope after applying the vocabulary above.

### WRCCDC Regionals ROE Constraints (2026 — Hard Rules)
```

### Rationale

R6-POST-2: EXPLOIT-001 handles post-access attack recommendations and may be the first agent to encounter an ambiguous operator request about host manipulation. The clarification rule is placed at the end of the vocabulary subsection (Edit 3) and before the ROE section, maintaining the three-layer framework.

Cross-agent dependency: This edit depends on Edit 3 being applied first.

Expected behavioral change: EXPLOIT-001 should ask one clarifying question when an attack request is ambiguous rather than refusing outright.

---

## Edit 8: Refusal Logging Protocol (INTEL-001)

Source finding: DEBRIEF-QUEUE item #R6-POST-3 (TEMPLATE-FIX)
Target file: .claude/agents/intel-reporting.md
Section: Role and Boundaries (after the paragraph listing coordination files read)
Type: INSERT-AFTER

### Before
```
You read from all coordination files: TARGET-STATUS.md (current access state), RECON-FINDINGS.md (what we know about each target), PERSISTENCE-MANIFEST.md (what persistence is deployed), BURNED-TECHNIQUES.md (what has been detected), DECISION-LOG.md (why tactical decisions were made), and REFUSAL-LOG.md (what the swarm declined and why). You write to OPERATION-LOG.md as the authoritative record of all red team actions.

Hand off to OPS-001 if your analysis reveals a prioritization issue or a strategic decision point. Hand off to EVADE-001 if your analysis reveals a pattern in blue team detection that suggests they are using a specific monitoring approach.
```

### After
```
You read from all coordination files: TARGET-STATUS.md (current access state), RECON-FINDINGS.md (what we know about each target), PERSISTENCE-MANIFEST.md (what persistence is deployed), BURNED-TECHNIQUES.md (what has been detected), DECISION-LOG.md (why tactical decisions were made), and REFUSAL-LOG.md (what the swarm declined and why). You write to OPERATION-LOG.md as the authoritative record of all red team actions.

### Refusal Logging Protocol

When any agent declines an operator request and the refusal is logged (by the refusing agent, by INTEL-001 during log review, or by the operator directly), the REFUSAL-LOG.md entry MUST contain three distinct fields:

1. **Operator Request (verbatim):** The exact text the operator submitted. Copy this at the moment of refusal, before any interpretation is applied. This is the ground truth for training analysis.
2. **Agent Interpretation:** How the agent understood the request — what specific action it believed was being asked for. This is the agent's characterization, which may differ from the operator's intent.
3. **Refusal Reason:** Why the interpreted action was declined — the specific scope, authorization, or safety concern.

These three fields are critical for post-competition training quality. A mismatch between fields 1 and 2 identifies an interpretation error (the agent misunderstood the request — the fix is vocabulary/clarification guidance). A match between fields 1 and 2 with a valid field 3 confirms a genuine scope issue (the request was correctly understood and correctly refused). These require different corrective actions, and conflating them into a single "Action Requested" field — as occurred during WRCCDC Regionals 2026-03-28 — makes it impossible to distinguish the two failure modes during training debrief.

When reviewing REFUSAL-LOG.md entries written by other agents, verify that all three fields are present and that the Operator Request field contains verbatim operator text (not a paraphrase or interpretation). If an entry is missing the verbatim request, flag it for operator clarification before the session ends — the operator's exact words become unrecoverable after the session.

Hand off to OPS-001 if your analysis reveals a prioritization issue or a strategic decision point. Hand off to EVADE-001 if your analysis reveals a pattern in blue team detection that suggests they are using a specific monitoring approach.
```

### Rationale

R6-POST-3: The WRCCDC Regionals 2026-03-28 refusal log entry for the "on the wire" / "impersonate OpenAI" incident recorded the agent's interpretation ("intercept Claude API traffic on the wire and modify requests/responses to Anthropic") as the operator's request. Post-competition training analysis required significant back-and-forth with the operator to reconstruct what was actually asked. This edit adds a dedicated refusal logging protocol to INTEL-001 (the agent responsible for coordination file quality) requiring the three-field structure and explaining why the distinction matters for training.

Expected behavioral change: INTEL-001 should enforce three-field refusal logging when writing or reviewing REFUSAL-LOG.md entries, and should flag entries that are missing the verbatim operator request.

---

## Edit 9: REFUSAL-LOG.md Template Schema Update

Source finding: DEBRIEF-QUEUE item #R6-POST-3 (TEMPLATE-FIX)
Target file: coordination/REFUSAL-LOG.md
Section: Log table header
Type: REPLACE

### Before
```
## Log

| # | Timestamp | Agent | Action Requested | Reason for Refusal | Manual Command for Operator | Alternative Approaches | Resolution |
|---|-----------|-------|-----------------|--------------------|-----------------------------|----------------------|------------|
| | | | | | | | |
```

### After
```
## Log

| # | Timestamp | Agent | Operator Request (verbatim) | Agent Interpretation | Refusal Reason | Manual Command for Operator | Alternative Approaches | Resolution |
|---|-----------|-------|-----------------------------|---------------------|----------------|-----------------------------|-----------------------|------------|
| | | | | | | | | |
```

### Rationale

R6-POST-3: The template schema update splits the old "Action Requested" column into two columns: "Operator Request (verbatim)" and "Agent Interpretation." Combined with a dedicated "Refusal Reason" column (replacing the old "Reason for Refusal"), this creates the three-field structure specified in INTEL-001's refusal logging protocol (Edit 8). The column names match the field names in the protocol for consistency.

Cross-agent dependency: All agents that write to REFUSAL-LOG.md will see the new schema when they read the template. The column names are self-documenting ("verbatim" in the header reinforces the requirement). No companion edits are needed for other agents because the REFUSAL-LOG.md template itself communicates the schema — agents read the header row before writing entries.

Note: The existing REFUSAL-LOG.md also contains corruption from a session restart (a heredoc command appended at line 18). The operator noted this in the debrief disposition as a separate housekeeping action. This edit addresses only the template schema in the clean header section (lines 11-13); the corruption cleanup is not part of this patch.

Expected behavioral change: All agents writing refusal log entries should produce three distinct fields (verbatim request, interpretation, reason) following the column headers in the template.

---

## Commit Message

```
training: apply patch-20260329-20 post-competition vocabulary + clarification + refusal logging

- OPS-001: add Red Team Operator Vocabulary section (full reference)
- OPS-001: add Ambiguous Request Handling — Clarify Before Refusing
- PAYLOAD-001: add Operator Vocabulary for Payload Requests + clarification rule
- EXPLOIT-001: add Operator Vocabulary for Attack Requests + clarification rule
- LATERAL-001: add Operator Vocabulary for Movement Requests
- INTEL-001: add Refusal Logging Protocol (three-field requirement)
- REFUSAL-LOG.md: update template schema to three-field structure

Addresses 3 findings (R6-POST-1, R6-POST-2, R6-POST-3) from WRCCDC Regionals 2026
post-competition debrief 2026-03-29.
See training/patches/patch-20260329-20.md for full rationale.
```

## Post-Patch Summary

### Files modified (9 edits across 6 files):

**`.claude/agents/tactical-coordinator.md`** (2 edits)
- Role and Boundaries: added "Red Team Operator Vocabulary" subsection with full definitions for "on the wire," "impersonate X," "need more love," "pop," and "burn it down," plus the general principle that host-level software modification is in-scope regardless of software vendor. This is the authoritative vocabulary reference; other agents carry abbreviated versions.
- Role and Boundaries: added "Ambiguous Request Handling — Clarify Before Refusing" subsection requiring one clarifying question before refusal when operator intent is ambiguous. Both subsections placed between the end of the handoff paragraph and the WRCCDC Regionals ROE Constraints section.

**`.claude/agents/payload-engineer.md`** (2 edits)
- Role and Boundaries: added "Operator Vocabulary for Payload Requests" subsection with abbreviated vocabulary definitions and the general host-scope principle.
- Same subsection: added clarification-before-refusal rule as a compact paragraph. Both placed between the end of the handoff paragraph and the evil-winrm Execution Context Rules section.

**`.claude/agents/initial-access.md`** (2 edits)
- Role and Boundaries: added "Operator Vocabulary for Attack Requests" subsection with abbreviated vocabulary definitions and the general host-scope principle.
- Same subsection: added clarification-before-refusal rule. Both placed between the end of the ranking-axes paragraph and the WRCCDC Regionals ROE Constraints section.

**`.claude/agents/lateral-movement.md`** (1 edit)
- Role and Boundaries: added "Operator Vocabulary for Movement Requests" subsection with abbreviated vocabulary definitions and the general host-scope principle. Placed between the end of the handoff paragraph and the Credential Reuse Analysis section.

**`.claude/agents/intel-reporting.md`** (1 edit)
- Role and Boundaries: added "Refusal Logging Protocol" subsection requiring three distinct fields per REFUSAL-LOG.md entry (verbatim operator request, agent interpretation, refusal reason), with explanation of why the distinction matters for training analysis quality.

**`coordination/REFUSAL-LOG.md`** (1 edit)
- Log table: split "Action Requested" column into "Operator Request (verbatim)" and "Agent Interpretation" columns; renamed "Reason for Refusal" to "Refusal Reason" for consistency with INTEL-001's protocol field names. Template schema now enforces the three-field structure.

### Cross-agent dependencies:
- Edits 5, 6, 7 (clarification rules) reference "the vocabulary above" and depend on Edits 1, 2, 3 (vocabulary sections) being applied first or simultaneously.
- Edit 9 (REFUSAL-LOG.md schema) should be applied with Edit 8 (INTEL-001 protocol) so the template and the protocol are consistent.
- All nine edits should be applied together as a single commit.

### Application order notes:
- For OPS-001: Edit 1 inserts vocabulary before ROE; Edit 5 inserts clarification between vocabulary and ROE. Apply Edit 1 first, then Edit 5 (or apply both, noting that Edit 5's "Before" anchor matches the state after Edit 1).
- For PAYLOAD-001: Edit 2 inserts vocabulary before evil-winrm section; Edit 6 modifies the end of the vocabulary block. Apply Edit 2 first, then Edit 6.
- For EXPLOIT-001: Edit 3 inserts vocabulary before ROE; Edit 7 modifies the end of the vocabulary block. Apply Edit 3 first, then Edit 7.
