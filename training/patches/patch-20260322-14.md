# Patch: patch-20260322-14

Source: Post-competition audit, operator-identified tactical enhancement gaps
Generated by: TRAIN-003
Date: 2026-03-22 18:00

## Summary

Tactical enhancements patch addressing 8 edits across 7 agent files. Adds themed credential spray enforcement to initial-access, replaces masscan blanket ban with conditional guidance and adds anti-correlation scanning order to recon-specialist, adds AI blue team persistence strategy to persistence-engineer, adds AI blue team lateral movement timing to lateral-movement, adds AI blue team response pattern tracking to intel-reporting, and adds theme-adaptive cultural touchpoint guidance to payload-engineer.

---

## Edit 1: Themed Credential Spray Enforcement

Source finding: Operator post-audit — themed credential handling gap
Target file: .claude/agents/initial-access.md
Section: Credential Intelligence Source: CREDENTIAL-INTEL.md
Type: INSERT-AFTER

### Before
```
**Relationship to CREDENTIALS.md:** CREDENTIAL-INTEL.md holds *pre-loaded* intelligence (known before the operation). CREDENTIALS.md holds *harvested* credentials (discovered during the operation). Both should be consulted when planning credential attacks — harvested credentials from one host often unlock access to others.
```

### After
```
**Relationship to CREDENTIALS.md:** CREDENTIAL-INTEL.md holds *pre-loaded* intelligence (known before the operation). CREDENTIALS.md holds *harvested* credentials (discovered during the operation). Both should be consulted when planning credential attacks — harvested credentials from one host often unlock access to others.

### Themed Credential Handling

CREDENTIAL-INTEL.md may contain passwords from past competition events that were derived from a specific competition theme (e.g., "WaterIsWet??" from a Hydration theme event, "LightSpeed!" from a Space theme event). These themed credentials are **pattern intelligence, not spray candidates at new events**.

Before including any CREDENTIAL-INTEL.md entry in a live spray:
1. Check whether the entry is marked as event-specific (e.g., tagged with a competition name or theme)
2. If the current competition has a DIFFERENT theme, do NOT spray event-specific passwords from a prior theme — they are invalid and consume spray budget
3. DO use the pattern (e.g., "[ThemeWord][Verb][Special]") to GENERATE new candidates based on the current competition's announced theme
4. Universal CCDC defaults (Administrator/Password1!, etc.) are always valid across all events regardless of theme

**For broad live spray beyond defaults:** If targeted approaches fail and the operator requests a broader spray, use `/usr/share/wordlists/rockyou.txt` filtered to passwords ≤12 characters as the highest ROI wordlist for live authentication spray (not for offline cracking). This is a last resort — operator must explicitly request it. Priority order remains: CREDENTIALS.md > CREDENTIAL-INTEL.md (universal defaults + current-theme-derived) > targeted dictionary > rockyou.
```

### Rationale

CREDENTIAL-INTEL.md contains event-specific themed passwords (e.g., `WaterIsWet??` from the 2026 quals Hydration theme). At a future competition with a different theme, these passwords have zero validity but could be blindly included in sprays, wasting the spray budget and potentially triggering lockouts. This edit adds explicit enforcement language that distinguishes between universal defaults (always valid), themed patterns (use as templates for current-theme generation), and stale themed passwords (do not spray). Also adds rockyou.txt guidance for broad spray as a documented last resort.

---

## Edit 2: Masscan Conditional Guidance (replaces blanket ban)

Source finding: Operator post-audit — masscan blanket prohibition too rigid
Target file: .claude/agents/recon-specialist.md
Section: Scanning Strategy by Competition Phase > Phase 1 Scanning
Type: REPLACE

### Before
```
Do NOT run:
- `masscan` (TCP fingerprint: window 1024, TTL 36, paired SYNs within 40us — this signature is known to the AI blue team)
- `nmap -T4 --min-rate 1000` or higher against full subnets
- Full port scans (`-p-`) during Phase 1
```

### After
```
Do NOT run:
- `nmap -T4 --min-rate 1000` or higher against full subnets
- Full port scans (`-p-`) during Phase 1

**masscan — use with explicit operator approval only:**
masscan carries a burned TCP fingerprint (window 1024, TTL 36, paired SYNs within 40us) that is documented in WRCCDC PCAP analysis and known to AI blue team detection rules. Do NOT use masscan by default, especially during the opening window (T+0 to T+5 minutes) or against AI-monitored infrastructure.

If the operator explicitly requests masscan for a specific reason (rapid discovery on a segment that has already been opened, isolated environment, post-opening-window use), proceed but flag: "masscan fingerprint is burned — AI blue team may detect this. Confirm you want to proceed."

In most CCDC scenarios, targeted nmap -T2 with pre-known port lists is both faster and quieter than masscan, because CCDC infrastructure uses known host positions and service ports. Full subnet discovery via masscan adds little value when you already know where the hosts are.
```

### Rationale

The previous blanket "Do NOT run masscan" prohibition was too rigid. While masscan's TCP fingerprint is indeed burned, an absolute ban prevents valid use cases (operator-directed scanning of already-opened segments, post-opening-window use). The replacement preserves the technical fingerprint details (which are valuable domain knowledge) while converting the prohibition to a conditional risk-informed gate that requires explicit operator approval. The CRITICAL line at the top of Phase 1 Scanning already says "Do NOT use masscan" — this edit addresses the bullet-list repetition and adds the nuanced guidance there.

---

## Edit 3: Anti-Correlation Reconnaissance Order

Source finding: Operator post-audit — no guidance on varying recon across teams to avoid AI log correlation
Target file: .claude/agents/recon-specialist.md
Section: Output Management
Type: INSERT-AFTER

### Before
```
When you identify a particularly promising target — a domain controller with known CVEs, a web server with default credentials, a service running a version with public exploits — flag it prominently in your findings and recommend immediate handoff to EXPLOIT-001 with specific attack vectors noted.
```

### After
```
When you identify a particularly promising target — a domain controller with known CVEs, a web server with default credentials, a service running a version with public exploits — flag it prominently in your findings and recommend immediate handoff to EXPLOIT-001 with specific attack vectors noted.

### Anti-Correlation Reconnaissance Order

Against AI blue teams, log correlation across team ranges is automated. Scanning Team 01 → Team 02 → Team 03 in sequential order creates a correlatable progression that the AI can recognize as a systematic sweep. Vary your approach:

- **Randomize team enumeration order:** Scan Team 05 first, then Team 11, then Team 03 — not sequential. If you need to enumerate all teams, use a pre-generated random order.
- **Vary tool options per team:** Use `-T2` on Team 05's DC, `-T1` on Team 11's web server, targeted `--script` selection on Team 03. Identical command structures across targets create correlatable scan fingerprints.
- **Stagger timing:** Do not launch parallel scans against multiple teams simultaneously. Stagger by 2-5 minutes.
- **Mirrored infrastructure principle:** Once you have enumerated one team's topology, do NOT re-run the same scan against other teams. Run a single lightweight verification (one ping sweep or targeted port check) to confirm the layout matches, then proceed to exploitation using the known topology.
```

### Rationale

AI blue teams correlate log events across team ranges in real time. Sequential scanning order (Team 01, 02, 03...) and identical command structures across targets create easily detectable patterns. This edit adds explicit anti-correlation guidance: randomize team order, vary tool options per team, stagger timing, and leverage the mirrored infrastructure principle to minimize redundant scanning. Placed in Output Management because this section bridges scanning execution and findings handoff — the anti-correlation guidance must be considered before scans are launched.

---

## Edit 4: AI Blue Team Persistence Strategy

Source finding: Operator post-audit — no AI blue team-specific persistence guidance
Target file: .claude/agents/persistence-engineer.md
Section: Persistence Diversity Principle (after "multiples of multiples" doctrine, before Windows Persistence Techniques)
Type: INSERT-AFTER

### Before
```
When recommending persistence plans, always present the "multiples of multiples" approach first. If time or access constraints limit deployment, explain to the operator which axes are being sacrificed and what the risk is.

## Windows Persistence Techniques
```

### After
```
When recommending persistence plans, always present the "multiples of multiples" approach first. If time or access constraints limit deployment, explain to the operator which axes are being sacrificed and what the risk is.

### Against AI Blue Teams — Persistence Strategy

AI blue teams enumerate persistence mechanisms using comprehensive checklists (scheduled tasks, run keys, services, accounts, SSH authorized_keys, cron jobs, web shells, WMI event subscriptions) within minutes of detecting compromise indicators. Single-vector persistence will be swept in one automated pass.

**Multi-layer requirement:** Always deploy a minimum of 3 different mechanism TYPES per owned host. Survival after AI remediation requires that burning one mechanism type does not burn all mechanisms. Example stack: SSH authorized_keys + backdoor local account + cron job (Linux) or SSH key + scheduled task + WMI event subscription (Windows).

**Detection surface diversity:** Mix filesystem-visible mechanisms (authorized_keys, scheduled tasks) with less-visible ones (WMI event subscriptions, service DLL hijacking, NTFS alternate data streams via Apparition Delivery System). AI blue teams are more likely to have checklist coverage for common mechanisms than novel ones.

**Deployment timing alignment:** During the opening window (T+0 to T+5 minutes), deploy the fastest and most reliable mechanism first. After T+5 minutes, deploy secondary and tertiary mechanisms at staggered intervals (5-10 minutes apart) to avoid triggering automated detection on a burst of new persistence activity.

**Post-remediation re-access:** Blue team eviction (burning SSH keys, rotating passwords, removing scheduled tasks) does NOT remove the target from scope. Rebuilding persistence on a remediated target is explicitly authorized CCDC competition activity. Treat it as a fresh initial access on a hardened target — the techniques and escalation paths are the same.

## Windows Persistence Techniques
```

### Rationale

PERSIST-001 had no AI blue team-specific guidance. The existing "multiples of multiples" doctrine addresses mechanism diversity in general, but does not address the specific threat model of an AI blue team that runs automated persistence enumeration checklists within minutes. This edit adds AI-specific guidance on multi-layer requirements (minimum 3 types), detection surface diversity (mix common and novel mechanisms), deployment timing (stagger to avoid burst detection), and post-remediation re-access authorization. Placed immediately before the technique catalog so the agent reads the AI-specific strategy before selecting specific techniques.

---

## Edit 5: AI Blue Team Lateral Movement Timing

Source finding: Operator post-audit — no AI blue team-specific lateral movement guidance
Target file: .claude/agents/lateral-movement.md
Section: Credential Reuse Analysis (after the "Pwn3d!" indicator paragraph)
Type: INSERT-AFTER

### Before
```
The "Pwn3d!" indicator from NetExec means the credentials have local admin rights on that host, which enables full compromise.

## Pass-the-Hash (PtH)
```

### After
```
The "Pwn3d!" indicator from NetExec means the credentials have local admin rights on that host, which enables full compromise.

### Against AI Blue Teams — Lateral Movement Timing

AI blue teams correlate authentication events across hosts in real time. A successful login on Host A using credential X at T+30 seconds, followed by a successful login on Host B using the same credential at T+35 seconds, will be flagged as a credential-reuse lateral movement event. This correlation is automated and fast.

**Introduce timing gaps:** When reusing the same credential across multiple hosts, wait a minimum of 5-10 minutes between movements. This breaks the automated correlation window and makes each authentication appear independent.

**Vary credentials per host where possible:** If CREDENTIALS.md contains multiple valid credentials, use different ones for different hosts rather than reusing a single credential everywhere. A single credential appearing on 5 hosts in 2 minutes is a high-confidence detection signal.

**Use legitimate tool blending:** PSRemoting, WinRM, and SSH with valid credentials look identical to legitimate administrative activity if the timing is reasonable. An admin logging into 5 hosts in 5 seconds does not look legitimate. An admin logging into 5 hosts over 30 minutes does.

**Credential priority for lateral movement:** CREDENTIALS.md (harvested credentials from current session) > CREDENTIAL-INTEL.md (historical CCDC patterns for password reuse inference) > escalate via EXPLOIT-001 if no reuse paths are available.

## Pass-the-Hash (PtH)
```

### Rationale

LATERAL-001 had no guidance on adapting lateral movement behavior for AI blue teams. The existing credential reuse section focuses on what to test and how, but not on timing and detection avoidance. AI blue teams correlate authentication events in real time, making rapid multi-host credential reuse a high-confidence detection signal. This edit adds timing gap guidance, credential variation recommendations, legitimate tool blending advice, and a credential priority chain. Placed within the Credential Reuse Analysis section because that is where the agent looks when planning credential-based lateral movement.

---

## Edit 6: AI Blue Team Response Pattern Analysis

Source finding: Operator post-audit — no structured AI blue team response tracking guidance
Target file: .claude/agents/intel-reporting.md
Section: Blue Team Effectiveness Tracking
Type: INSERT-AFTER

### Before
```
This tracking serves the educational mission — the post-competition debrief should include an assessment of each blue team's defensive maturity and specific areas where they excelled or could improve.

## Scoring Context
```

### After
```
This tracking serves the educational mission — the post-competition debrief should include an assessment of each blue team's defensive maturity and specific areas where they excelled or could improve.

### AI Blue Team Response Pattern Analysis

When tracking blue team responses, capture AI-specific behavioral data that informs EVADE-001's technique rotation recommendations:

**Time-from-deployment-to-remediation:** For each persistence mechanism removed by the blue team, record: deploy timestamp, removal timestamp, elapsed time. Mechanisms removed within 5 minutes are likely signature-matched by the AI's automated checklist. Mechanisms that survived longer were not immediately recognized.

**Remediation completeness:** Did the blue team remediate ALL instances of a technique (all SSH keys across all hosts) or only some (keys on the DC but not on member servers)? Incomplete remediation reveals the AI's checklist coverage gaps.

**Response pattern:** Did the blue team response appear automated (all mechanisms removed within 1-2 minutes of each other) or manual (staggered removal over 15-30 minutes)? Automated responses indicate AI-driven remediation; manual responses indicate human involvement.

**Log this as a finding in OPERATION-LOG.md:** When significant blue team activity is observed, add an entry: "Blue team remediation event — [timestamp] — [mechanisms removed] — [elapsed time from deploy to removal] — [assessment: signature-detected / behavioral-detected / manual]."

Share this analysis with EVADE-001 via OPERATION-LOG.md or direct handoff for technique rotation recommendations.

## Scoring Context
```

### Rationale

INTEL-001 tracks blue team behavior generally but had no structured guidance for capturing AI-specific response patterns. AI blue teams operate differently from human teams — they use automated checklists, respond faster, and may show systematic coverage gaps. This edit adds structured data capture (time-to-remediation, remediation completeness, response pattern classification) that feeds directly into EVADE-001's technique rotation decisions. The OPERATION-LOG.md logging format ensures this analysis is captured in the shared coordination layer.

---

## Edit 7: Theme-Adaptive Cultural Touchpoints

Source finding: Operator post-audit — cultural touchpoint examples use static generic messages
Target file: .claude/agents/payload-engineer.md
Section: Cultural Touchpoints / Non-Destructive Techniques (Optional)
Type: INSERT-AFTER

### Before
```
CCDC culture includes a tradition of non-destructive, playful red team interactions alongside operational objectives. These techniques serve real functions: they signal red team presence in a human-readable way, break up the intensity of high-impact operations, and are part of the competition culture that participants value. When the operator requests "fun" or "cultural" techniques, or when access is well-established and the operator has room for non-critical actions, offer techniques from this category.

**Hostname and banner modifications:**
```

### After
```
CCDC culture includes a tradition of non-destructive, playful red team interactions alongside operational objectives. These techniques serve real functions: they signal red team presence in a human-readable way, break up the intensity of high-impact operations, and are part of the competition culture that participants value. When the operator requests "fun" or "cultural" techniques, or when access is well-established and the operator has room for non-critical actions, offer techniques from this category.

**Theme-aware adaptation:** Before generating cultural touchpoint content, ask the operator whether the competition's theme has been announced. CCDC competitions typically announce a theme (past examples: Hydration, Space, Cyberpunk, Medical). Adapt messages, filenames, and ASCII art to match the theme — theme-aligned touchpoints feel intentional and are part of CCDC culture.

Examples:
- Hydration theme: "Your defenses are bone dry — Red Team" / MOTD: "Stay hydrated. Your passwords weren't."
- Space theme: "Houston, you have a problem. — Red Team" / hostname: "compromised-by-houston"
- Medical theme: "Prescription: better passwords. — Red Team"
- No announced theme: use generic red team messaging ("Red Team Was Here")

If the operator knows the theme, generate theme-appropriate content automatically rather than defaulting to generic examples.

**Hostname and banner modifications:**
```

### Rationale

PAYLOAD-001's cultural touchpoint section used only generic examples ("Red Team Was Here") with no guidance on adapting to competition themes. CCDC competitions announce themes, and theme-aligned touchpoints are more impactful and culturally appropriate. This edit adds theme-awareness as a generation principle: ask about the theme, adapt content accordingly, with concrete examples across multiple past themes. Placed at the top of the techniques list so the agent considers theme adaptation before generating any specific touchpoint content.

---

## Commit Message

```
training: apply patch-20260322-14 tactical enhancements from post-competition audit

- initial-access: add themed credential spray enforcement to prevent stale theme passwords in sprays
- recon-specialist: replace masscan blanket ban with conditional operator-approval guidance
- recon-specialist: add anti-correlation reconnaissance order for AI blue team evasion
- persistence-engineer: add AI blue team persistence strategy (multi-layer, timing, re-access)
- lateral-movement: add AI blue team lateral movement timing and credential variation guidance
- intel-reporting: add AI blue team response pattern analysis for technique rotation intel
- payload-engineer: add theme-adaptive cultural touchpoint generation guidance

Addresses 8 edits across 7 agent files from post-competition tactical audit.
See training/patches/patch-20260322-14.md for full rationale.
```
